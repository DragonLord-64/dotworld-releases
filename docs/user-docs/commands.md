<!-- GENERATED by scripts/build-user-docs.mjs — do not edit. Run `npm run docs`. -->

# Command reference

Every command the shipped build dispatches, every parameter it declares, and — for most —
a real captured session. Nothing on this page is hand-written: the descriptions come from the
same `meta.params` that generates `dot --help`, and each example was **executed** against a
throwaway repository at build time. What you see under `$ dot …` is what the CLI actually printed.

The examples are **one session**, run start to finish against a single small source, so
an output can depend on a call shown earlier in the session. The page groups them by
what they are for rather than replaying that order, so a section further down may show
a source further along than the section above it. The repository is a four-file handbook:

- `notes/retention.md`
- `notes/deletion-runbook.md`
- `notes/onboarding.md`
- `decisions/2026-02-adopt-postgres.md`

A few examples show a **refusal** rather than a success. Those are marked, and they are
there on purpose: what going wrong looks like is part of the interface.

## Reading a result

Every command answers with the same envelope — `{ "ok": true, "result": … }` or
`{ "ok": false, "error": …, "code": … }`. Advisory notes (which tree answered, what was
inferred) go to **stderr**, so piping stdout to `jq` stays clean.

A field shown as `"<varies>"` was **redacted, not invented**: build stamps, timestamps and
the staleness scores `search_triage` ranks on change between any two runs, so pinning them
to a plausible-looking number would print a value that is true for nobody. Everything else
on this page is verbatim.

An output shown as `… N more lines of output` was **truncated for the page**, never by the
CLI. Run the same call yourself and you get the whole thing.

---

## Contents

**[Sources and syncing](#sources-and-syncing)** — [`source_list`](#source_list) · [`source_register`](#source_register) · [`source_unregister`](#source_unregister) · [`source_update`](#source_update) · [`sync_doctor`](#sync_doctor) · [`sync_merge`](#sync_merge) · [`sync_source`](#sync_source)

**[Dots](#dots)** — [`dot_attach`](#dot_attach) · [`dot_get`](#dot_get) · [`dot_list`](#dot_list) · [`dot_set`](#dot_set)

**[Files](#files)** — [`file_create`](#file_create) · [`file_delete`](#file_delete) · [`file_read`](#file_read) · [`file_stage`](#file_stage) · [`file_tree`](#file_tree)

**[Search](#search)** — [`search_grep`](#search_grep) · [`search_properties`](#search_properties) · [`search_semantic`](#search_semantic) · [`search_triage`](#search_triage)

**[Properties](#properties)** — [`property_get`](#property_get) · [`property_list`](#property_list) · [`property_register`](#property_register) · [`property_set`](#property_set) · [`property_unregister`](#property_unregister) · [`property_unset`](#property_unset)

**[Indexes and context](#indexes-and-context)** — [`context_load`](#context_load) · [`index_create`](#index_create)

**[The graph](#the-graph)** — [`graph_affected`](#graph_affected) · [`graph_connect`](#graph_connect) · [`graph_disconnect`](#graph_disconnect) · [`graph_get`](#graph_get) · [`graph_provenance`](#graph_provenance)

**[Workspaces and messaging](#workspaces-and-messaging)** — [`comms_read`](#comms_read) · [`comms_send`](#comms_send) · [`workspace_close`](#workspace_close) · [`workspace_list`](#workspace_list) · [`workspace_open`](#workspace_open) · [`workspace_submit`](#workspace_submit)

**[Summaries and obsolescence](#summaries-and-obsolescence)** — [`obsolescence_review`](#obsolescence_review) · [`summary_refresh`](#summary_refresh)

**[Version control](#version-control)** — [`git`](#git)

**[This install](#this-install)** — [`embedding_info`](#embedding_info) · [`plugin_list`](#plugin_list) · [`version`](#version)

**[The CLI's own subcommands](#the-clis-own-subcommands)** — [`commands`](#commands) · [`catalog`](#catalog) · [`completion`](#completion) · [`help`](#help)

---

## Sources and syncing

## `source_list`

List registered sources (id and root) — the source-side of plugin_list.

_No parameters._

```console
$ dot source_list
{"ok":true,"result":[{"id":"handbook","root":"/path/to/handbook"}]}
```

---

## `source_register`

Point the kernel at a repo directory as a source (one source = one repo). `root` MUST BE the repository's toplevel — a subdirectory of a repo is refused, and so is a directory that is not a git repository at all (use `initGit=true`, or `git init` it yourself, to make it one). A knowledge base living inside a larger project needs its own repository. THERE IS NO BRANCH TO REGISTER: dotworld holds no landing target. A workspace forks from wherever the repo's canonical checkout has its HEAD, and `workspace_submit` merges back into that same checkout, wherever it is standing at the time. To change where work lands, `git checkout` there — `workspace_list` reports the canonical checkout and its branch.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | string | yes | source id |
| `root` | string | yes | absolute path to the source directory — must be a git repo's toplevel |
| `initGit` | boolean |  | initialise a git repo if absent. The branch name is git's own (`init.defaultBranch`, or whatever your git defaults to) — dotworld does not choose one |
| `neverPushDotworld` | boolean |  | DISABLED (#404). Passing this at all is REFUSED, either value; the refusal says what to do instead. Nothing filters a push: `.gitignore` `.dotworld/` if you want it out of a remote. |
| `writeAgentsDoc` | boolean |  | append a short dotworld orientation block to the source's `AGENTS.md`, creating the file if absent — so the first agent working in a freshly registered repo knows the substrate is there. Off by default: this writes into YOUR repository. No-op if a `## dotworld` heading is already present, so re-registering never duplicates it. The write is uncommitted, and the answer reports `created`, `appended` or `already-present` |

```console
$ dot source_register id=handbook root=.
{"ok":true,"result":{"id":"handbook","root":"/path/to/handbook"}}
```

---

## `source_unregister`

Forget where a source lives — the inverse of `source_register`. THIS DELETES NOTHING ON DISK. A source root is your repository; unregistering is forgetting where it is, not removing it. Your files stay, and so does the `.dotworld/` directory inside them — the dots are durable, in git, and belong to the repo rather than to the registry, so registering the same root again brings every title, tag and property back. The disposable `.dotworld/derived/` index is left too, deliberately: a warm re-register is not worth giving the one command that destroys nothing a way to destroy something. The answer names the root, which is what you need to re-register, and `workspaces`: any workspaces this source has are NOT removed, and `workspace_close` needs a registered source, so close them first or you will have to re-register to reach them. A source DECLARED in `dotworld.config.json` is registered again on the next boot — remove the declaration, not just the registration.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | string | yes | the id of the source to forget |

Forgets where the source lives. THIS DELETES NOTHING in the repository — the files, the dots and the git history are all still there.

```console
$ dot source_unregister id=handbook
{"ok":true,"result":{"id":"handbook","root":"/path/to/handbook","workspaces":[],"stillVisible":false,"detail":"handbook is no longer registered. Nothing on disk was touched — re-register it with `source_register id=handbook root=/path/to/handbook` and its dots are still there."}}
```

---

## `source_update`

Repoint a registered source at a different `root`. THIS EDITS THE REGISTRY, NOT THE TREES: nothing on disk moves, and the directory a source used to point at is left exactly as it was. Three refusals worth knowing: an id that is not registered is refused rather than created (this is not a second door to `source_register`), a `root` that does not exist is refused rather than created — a mistyped path would otherwise repoint a live source at an empty directory and every command would then answer, truthfully, that there is nothing there — and a `root` that is not a git repo's toplevel is refused, same as `source_register` (one source = one repo). `root` is the only thing this changes. There is no branch to repoint: where work lands is what the canonical checkout has checked out, and `git checkout` is how you change it.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | string | yes | the id of the source to change |
| `root` | string |  | the new absolute path to the source directory (must already exist, and be a git repo's toplevel) |
| `neverPushDotworld` | boolean |  | DISABLED (#404). Passing this at all is REFUSED, either value; the refusal says what to do instead. Nothing filters a push: `.gitignore` `.dotworld/` if you want it out of a remote. |

Repoints the registry entry. Nothing on disk moves — this one repoints the source at the root it already had.

```console
$ dot source_update id=handbook root=.
{"ok":true,"result":{"id":"handbook","root":"/path/to/handbook"}}
```

---

## `sync_doctor`

Check this dotworld install for the ways it can be broken without saying so, and report each one with a verdict and the repair. One call covers: a merge driver that is registered but not runnable (reading the config key cannot tell — this runs it, the way git does, on synthetic files in a temp directory), a second driver registered under another name, an embedding engine that fell back, absent model weights, a build behind its source, a registered source whose root is gone, stranded dots, dots describing a file that is present and gitignored (so nothing indexes it and its metadata is in no answer — reported with the rule that excluded it, and separately from the stranded ones, because the repair is neither of theirs), dots that will not parse, titles naming a file that has been renamed away, authored edges pointing at nothing, connector files that will not parse or hold a weight the graph cannot use (each named with its reason — they are skipped silently otherwise, absent from graph_get and from every centrality score), worktree registrations orphaned by an rm -rf, an unresolved merge conflict anywhere in the source (asked of git directly — every other check here reads files on disk and cannot tell a conflicted path from a settled one), durable state under .dotworld/ (properties.json, connectors) that differs from what is committed — the silent gap `rm -rf .dotworld` leaves, which no sync can heal because nothing regenerates authored metadata — and a dot present on disk but never committed. Machine-wide: it takes no sourceId, because "is my install healthy" is not a question whose answer should change with your working directory — every check here holds that except two. The search-engine and model-weights checks report on whichever `dotworld.config.json` this process resolved (DOTWORLD_PLUGIN_CONFIG, else the machine config dir, else cwd — docs/plugins.md), so on a machine with no config in the machine config dir the answer follows the caller's cwd like every other cwd-resolved read. Both checks name the file (or say none was found) in their `detail`, so which config produced the answer is always stated rather than implied. Every check reports one of ok / warn / fail / unknown / skipped, and 'unknown' — a check that could not be established — keeps `ok` false, because a green verdict nobody established is the silent half-working this exists to end. `notChecked` names what it deliberately does not cover. It is a read: no staging, no `git config <key> <value>`, no indexing, no workspace created. `repair=true` is the only thing that writes, and only for the two repairs that re-assert state dotworld itself wrote — re-registering the merge driver and `git worktree prune`. Everything else reports the command to run and touches nothing.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `repair` | boolean |  | ALSO write the two repairs dotworld owns: re-register the merge driver, and prune worktree registrations whose directory is gone. Default false — a diagnostic that mutates on first run cannot be used to diagnose. Never repairs anything a human has to decide (a stranded dot may hold the only copy of some metadata) and never rewrites a line in your attributes file or a merge.* section it did not write. The report shows what was FOUND; re-run without the flag to confirm the fix. |

```console
$ dot sync_doctor
{"ok":true,"result":{"ok":true,"status":"ok","sources":["handbook"],"checks":[{"id":"build","scope":"machine","status":"ok","detail":"dist/ matches src/ (built <varies>, rev <varies>)."},{"id":"search-engine","scope":"machine","status":"ok","detail":"Search is running on 'fastembed', exactly as configured ('fastembed'). Config file in effect for this process: /path/to/handbook/.dotworld/dotworld.config.json."},{"id":"model-weights","scope":"machine","status":"ok","detail":"Weights for 'fast-all-MiniLM-L6-v2' are present in ~/.cache/dotworld/models. Config file in effect for this process: /path/to/handbook/.dotworld/dotworld.config.json."},{"id":"source-roots","scope":"machine","status":"ok","detail":"1 registered source(s), every root present."},{"id":"merge-driver-routing","scope":"handbook","status":"ok","detail":"*.dot.md is routed to 'dotworld-proxy', which merge.dotworld-proxy.driver defines."},{"id":"merge-driver-runnable","scope":"handbook","status":"ok","detail":"The registered driver ran and merged both sides of a probe dot (exit 0)."},{"id":"merge-driver-duplicates","scope":"handbook","status":"ok","detail":"Exactly one merge driver is registered, under 'dotworld-proxy'."},{"id":"orphan-dots","scope":"handbook","status":"ok","detail":"0 dots, none stranded."},{"id":"dots-for-ignored-files","scope":"handbook","status":"ok","detail":"Every dotted file that is present here is one this source indexes."},{"id":"dot-readable","scope":"handbook","status":"ok","detail":"Every one of 0 dots parses."},{"id":"dot-titles","scope":"handbook","status":"ok","detail":"No dot title looks like a filename this source no longer has."},{"id":"merge-conflicts","scope":"handbook","status":"ok","detail":"No unresolved merge conflict in this working tree."},{"id":"durable-state-committed","scope":"handbook","status":"ok","detail":"handbook's .dotworld/ (outside dots/) matches HEAD."},{"id":"unpersisted-dots","scope":"handbook","status":"ok","detail":"Every dot in 'handbook' matches what is committed."},{"id":"graph-edges","scope":"handbook","status":"ok","detail":"0 authored edge(s), every endpoint present."},{"id":"connector-portability","scope":"handbook","status":"ok","detail":"0 connector file(s), every name legal on every platform."},{"id":"worktrees","scope":"handbook","status":"ok","detail":"No stale worktree registrations."},{"id":"prepush-hook-stale","scope":"handbook","status":"ok","detail":"handbook carries no leftover neverPushDotworld artifacts (the feature is disabled)."}],"note":"Healthy: everything checked passed — 18 checks: 18 ok. Read the failing ones first; each carries its own repair.","repaired":false,"notChecked":["Derived links (wikilinks and markdown links in file bodies) that resolve to nothing: finding them needs a full index pass over the whole corpus, and this command is a read. Not detected anywhere today: `graph_get` and `sync_source` only report AUTHORED connectors, so a body link to a file that does not exist produces no error, no edge and no diagnostic — verified live, not merely asserted (#358).","`file_create`'s errno allowlist is POSIX-only, so on Windows a refusal it should translate surfaces raw. Not detectable by reading state on any platform — the fault only exists at the moment of a failed write.","Whether a plugin that declares `wrapsSubmit` behaves: `plugin_list` reports the composition, and only `workspace_submit` reaches that seam.","A DEAD `merge=` line in an attributes file this does not read. Which driver actually wins is `git check-attr`'s answer and is always right; but merge-driver-duplicates can only NAME a loser it can see, and it reads `info/attributes` in the git directory git itself names (#329), the source root's `.gitattributes` and the one beside the dots. A line in a deeper directory, in `core.attributesFile` or in the system file is invisible to that check.","A rename whose old filename stem was a single token (`runbook.md` → `procedures.md`) leaves a title dot-titles will not flag: the heuristic requires a `.`, `-` or `_`, because without one it fires on every ordinary one-word authored title and a warn keeps the whole report red."]}}
```

---

## `sync_merge`

Re-merge dots that a plain `git merge` left unreconciled, property by property. Use it when a merge reported conflict on a `.dot.md`, or when `dot_get` answers dotUnparseable: true. Name the files with `files`; omit it and every dot in the tree that needs it is found. Accepts any path and never refuses on a settled tree — this is a repair tool, not a merge-in-progress helper. It writes the reconciled dot and stages nothing: accepting the result is your own explicit `file_stage` or `git add`. Note that a dot damaged by a driver that failed carries no conflict markers at all — git leaves it well-formed with only one side in it — so "the file looks fine" is not evidence there is nothing to repair. Read the `outcome` of every row — it does not all mean the same thing. `merged` = settled and written. `conflicts` = a property changed on both sides with no strategy to settle it. On route `index` the dot was still written, and a side won: a registered scalar keeps theirs, an unregistered property keeps ours. `detail` names which properties and which side, and you should read it before you `git add` — that is the moment the winner becomes permanent. On route `markers` nothing is written at all and the file is left exactly as it is, markers and all, because that block is the only surviving copy of the other side. `delete-conflict` = git reports the dot unmerged as a modify/delete — one branch deleted it, the other edited it — so there is no second version to merge and nothing was written; `detail` says which side deleted and gives the two commands that settle it. `nothing-to-merge` = nothing was repaired, and is not success: a writer who already resolved a failed merge by staging their own side looks identical, and the side they dropped survives only in the merge commit's parents, which this does not read. `route` and `base` say how good the answer is: route `index` takes all three sides from git's index (base `real`, a full 3-way); route `markers` recovers them from the conflict markers in the file, and its base is `reconstructed` — every line outside the contested hunks — unless you run `merge.conflictStyle=diff3`, which puts the true base in the file and makes it `real`. A reconstructed base still unions tags correctly; what it cannot do is tell "both sides changed this scalar" from "the base never had it", so such a property conflicts rather than being guessed.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `files` | array of string |  | file paths whose dots to re-merge (default: every dot in the tree that needs it) |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

REPAIR: re-merge dots per property where a plain `git merge` left conflict markers. Ordinary submits do this for you — this is the door back in when a merge happened outside dotworld.

```console
$ dot sync_merge
{"ok":true,"result":[]}
```

---

## `sync_source`

Bring a source up to date: rebuild its disposable index from the files of one working tree, and heal a dot an older version wrote wrong (a missing `kind`, a title minted from a code comment). Run it after adding or renaming files. It MINTS NOTHING: a dot exists once there is something to record about a file — a tag, a property, a summary, a title you set, a body you wrote — and the write that records it is what brings the dot into being. A file with no dot is fully indexed, searchable and readable all the same; every read answers from an in-memory row and writes nothing. The index is per tree, so this rebuilds the tree it resolves — pass `workspace` to rebuild a workspace rather than the canonical checkout. Rebuilding also reconciles out-of-band renames: in a git source, a file you moved with `mv` or `git mv` keeps its dot — title, tags and properties — even if you edited it in the same sitting, both before and after you commit the move, and whether the source is the repository root or a directory inside it. Reconciliation itself runs on every read, not only here — you need not call this after moving a file for that part. In a non-git source only an unedited move is followed, because the content hash is the only evidence available. Where a move cannot be identified — too heavy a rewrite, an edited move outside git, or a move whose destination is outside this source (a file moved out of the tree has no destination here to carry a dot onto) — nothing is guessed: the moved file is described by a blank dot (this command writes it; a plain read answers with an equivalent one without saving it) and the old dot is left behind holding the real metadata. `dot_list orphaned=true` is where those leftovers are, with what each one holds and where git suggests the file went, and `dot_attach` reattaches one — so a file whose metadata came back empty after a move is a recovery, not a re-authoring. An advisory beside this answer counts them when there are any, because these counts are of live files and say nothing about a stranded dot.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

Rebuilds the disposable index and mints a `.dot.md` for every file that lacks one. A file added by hand is invisible to dotworld until this runs.

```console
$ dot sync_source sourceId=handbook
{"ok":true,"result":{"files":4,"connectors":2}}
```

---

## Dots

## `dot_attach`

Attach a stranded dot to a file — the manual half of rename reconciliation, for the move it could not pair. It relocates one dot; it does not relate two things, and it authors no edge — `graph_connect` is that command. Both params name files, never `.dot.md` paths: `dot` is the path the stranded dot still describes (as `dot_list orphaned=true` reports it), `file` is the file to attach it to. This does everything a detected rename does — rewrites the dot's `id` and `source_path`, moves the dot, and rewrites authored connectors naming the old path — which is why hand-moving the `.dot.md` is not equivalent: that silently severs every curated edge. Nothing is merged: the dot you name wins outright, because the reason you are here is that automatic pairing already guessed wrong once. Four refusals. (1) The named dot's own file still exists — nothing is stranded; move the file instead and the next read carries its dot. (2) `file` is not a file in this working tree. (3) The destination already has an authored dot — pass overwrite=true to replace it; a blank one, such as the one an interim read minted at the new path, is replaced without asking. A title counts as authored when dotworld did not mint it (a minted one is the filename stem or the file's own first H1), so a destination carrying nothing but a title somebody wrote is refused like any other; the refusal names it, and overwrite is all-or-nothing, so re-author it with `dot_set` afterwards if you wanted it kept. (4) Either dot is unparseable. The two halves of (4) differ, deliberately: a defective dot at the destination is replaceable with overwrite=true, because it is being discarded wholesale, but a defective dot at `dot` is refused outright and overwrite=true will not clear it — its `id` and `source_path` cannot be rewritten without reading it, so moving it would leave a broken dot describing a different file. Repair that one by hand first; the refusal names the repair.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `dot` | string | yes | the file path the stranded dot describes (NOT the .dot.md path) |
| `file` | string | yes | the file to attach that dot to |
| `overwrite` | boolean |  | replace an authored (or unparseable) dot at the destination (default false) |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

The manual half of rename reconciliation. `dot` is the path the stranded dot DESCRIBES, not the `.dot.md` path.

```console
$ dot dot_attach dot=notes/deletion-runbook.md file=notes/purge-runbook.md
{"ok":true,"result":{"from":"notes/deletion-runbook.md","to":"notes/purge-runbook.md","replaced":"blank","connectors":0}}
```

---

## `dot_get`

Read what dotworld knows about a file: the whole dot by default, or named components via `fields`. A dot carries title, tags, properties and an authorable body; the answer adds derived signals (centrality, summaryStatus). `centrality` is PageRank over the connector graph with `supersedes` edges left out — an edge recording that a document is dead is not evidence it matters — so a superseded file ranks on its other links alone. This is the triage step: it tells you whether a file is worth opening without paying for the file's body (that is `file_read`). The dot body is included by default — it is authored scratch notes about the file and is usually empty, so it costs nothing like `file_read`; `fields=["body"]` reads it alone. Name the files with `files`, or point `index` at a document and every file it links to is the list. Addressed by the file's path: identity is the path, and a dot is never addressed by its own. `dot_set` is the matching writer. For properties, prefer `property_get`: this command's projection omits a field the dot does not carry, so an absent property is indistinguishable from one you did not ask for — `property_get` names it in `missing` instead. `properties` is the trap, because it is not a field at all: property keys are spread at the top level of the answer, so `fields=["properties"]` answers `{"path":"…"}` for every file, always — name the property keys you want.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `files` | array of string |  | file paths or ids. Over the CLI this is CWD-relative, exactly like `git add` — MCP and HTTP always take it relative to the source root (#337). |
| `index` | string |  | a document whose LINKS are the list of files — use instead of (or as well as) `files`. Any document works, not a special format: a generated index from `index_create`, or a hand-written note with links in prose. Read from the file every time, so pruning a line changes the answer. External URLs and dead links are skipped rather than reported missing. Over the CLI this is CWD-RELATIVE, exactly like git (#337); MCP and HTTP always take it relative to the source root. |
| `fields` | array of string |  | return only these components — e.g. ["body"], ["tags","summary"], or any property key. Omit for the whole dot. `path` is always present, and asking for `summary` always brings `summaryStatus` with it. |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot dot_get 'files=["notes/retention.md"]'
notes/retention.md
title	Data retention policy
kind	prose
summary	unread
centrality	0.28804982483429553
```

`index=` works the same way on the dot side — every command taking `files` takes it. This is the cheap read: titles, tags and summaries for the whole set, without a single file body.

```console
$ dot dot_get index=notes/retention-pack.md
notes/retention.md
title	Data retention policy
tags	policy, compliance
kind	prose
summary	unread
centrality	0.2242466713385889
owner	alice
body	Reviewed each quarter by the data protection lead.

notes/deletion-runbook.md
title	Deletion runbook
kind	prose
summary	unread
centrality	0.3255080588644246
owner	bob

… 6 more lines of output
```

---

## `dot_list`

Every dot in this source as an object: the file it describes, where the `.dot.md` itself lives, and whether that file is still here. `orphaned=true` is the one that matters — a dot whose file is absent from the working tree. An orphan is durable, in git, and holds real authored metadata, and nothing else in the command set can see it: `file_tree` lists files, and every aggregate over dots (`search_properties`, `property_list` counts) is computed over live files only, so a property that exists solely on an orphan reads as used by nobody. A dot is orphaned in two ways, and the second is the common one: a file deleted on purpose leaves its dot behind (retire it with `git rm` of the `dotPath` here), and a file moved out of band leaves it behind when reconciliation could not pair the two halves (reattach it with `dot_attach`) — see `sync_source`. An orphan row carries the metadata stranded on it, so you can judge whether it is worth recovering without opening anything, plus `hint`/`hintReason`: where the file probably went, advisory and never acted on. Weigh the three reasons differently — `git-rename-in-the-deleting-commit` is git's own pairing of the two paths; `closest-embedding` is the nearest of that commit's arrivals to the orphan's last committed content in vector space, carrying `hintScore` (cosine, 0-1), which is how you tell a continuation from a coincidence; `the-only-file-added-in-that-commit` is mere co-occurrence: `git rm obsolete.md`, add `changelog.md`, commit, and the changelog is hinted, with nothing whatever to do with the file that left — and its `hintScore`, when present, is what says so. A near-tie gives no `hint` and a `hintTie` listing the candidates with their scores instead, because naming one would be a coin toss dressed as evidence. `semanticHints` says whether the embedding hint ran at all: with no embedding model loaded it declines rather than guessing — ask `embedding_info` why the model is not running. `orphaned` counts the stranded dots whether or not you filtered for them, so a plain call answers "is anything stranded?" in one go. The second state is `indexed`, and it is a different condition with a different repair: a file that is here (`exists: true`) yet is outside what this source tracks — which is what a file that acquired a dot and was then gitignored looks like. Its metadata is invisible to every query for the same reason an orphan's is, so `notIndexedReason` says why and `ignoredBy` names the rule as git names it: the ignore file, the line and the pattern. The repair is to change that rule or move the file, and it is neither orphan repair — nothing is stranded, `dot_attach` has nothing to reattach, and `git rm` would throw the metadata away. `notIndexed` is counted separately from `orphaned` for exactly that reason, and is likewise reported whether or not you filtered.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `orphaned` | boolean |  | only dots whose file is absent from this working tree (default false: every dot) |
| `notIndexed` | boolean |  | only dots whose file IS in this working tree and is not indexed — gitignored, usually (default false: every dot). Disjoint from `orphaned`, which selects dots with no file at all, so asking for both returns no rows; both counts report either way |
| `match` | string |  | case-insensitive substring of the FILE PATH the dot describes. This queries paths — `search_properties` is how you query property values |
| `limit` | number |  | maximum rows (default 200; negative for no limit). A source has one dot per file, so an unbounded call on a large one returns a row per file; `truncated: true` says the answer was cut short, and `total` says by how much |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot dot_list limit=3
notes/retention.md

1 dot, 0 orphaned, 0 not indexed
```

`match` is a case-insensitive substring of the FILE PATH the dot describes — a directory prefix, a filename fragment. It queries paths, never property values; `search_properties` is the command for those.

```console
$ dot dot_list match=notes/ limit=3
notes/retention.md

1 dot, 0 orphaned, 0 not indexed
```

A dot whose file is gone and whose rename could not be resolved is STRANDED. The old runbook was deleted and recreated under a new name in a separate commit, so git had no rename to detect — its title, tags and properties are still here, attached to nothing.

```console
$ dot dot_list orphaned=true
!notes/deletion-runbook.md

1 dot, 1 orphaned, 0 not indexed
```

Nothing stranded any more.

```console
$ dot dot_list orphaned=true

0 dots, 0 orphaned, 0 not indexed
```

---

## `dot_set`

Write what dotworld knows about a file: its `title`, `tags`, dot `body` and `properties`. Name only the components you want written — one you omit is left alone, never cleared, and `properties` names the keys to write rather than replacing the whole bag, so a narrow edit cannot silently discard metadata you did not mention. This is the setter matching `dot_get`, and the only validated way to change a `title` or `tags` once a file exists: `file_create` sets them at birth, and `property_set` refuses them, because writing one through a property bag overwrote the real field. The source file is never touched: metadata lives in the dot (use `file_create` to write file content). The dot is durable and in git, so the edit only survives once it is committed — dotworld never commits for you, in a workspace or the shared checkout alike: this writes the dot and leaves it uncommitted for you to land with `git` — a dot that never lands is invisible to every clone. `workspace_submit` refuses to land a workspace with uncommitted changes, naming them. `properties` is held to the same registry check as `property_set` — an unregistered key is refused, and the whole write is refused with it rather than half-applied. `title` and `tags` need no registration; they are not properties.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `file` | string | yes | file path or id — the FILE's path, as everywhere: a dot is never addressed by its own |
| `title` | string |  | replace the dot title |
| `tags` | array of string |  | replace the tag list outright (this is an authored edit, not a merge) |
| `body` | string |  | replace the dot's authorable body outright — freeform notes ABOUT the file (working notes, open questions, why-this-is-weird), never the file's own content. Within your tree this REPLACES what is there (an authored edit, not a merge), so read it first if you mean to add to it. ACROSS a divergence it is different: the body merges by line-union and never conflicts, so a note another writer added concurrently is kept beside yours. Five things that surprise callers. Their lines are RELOCATED to the end of the changed region, so a note written above a shared line can come out below it, and the merge is not commutative — two people merging the same branches can get different, both-valid bodies. Nothing is inserted at the JUNCTION between your lines and theirs — each writer's own blank lines survive, but neither wrote one there, so two stanzas can abut. Removing a line removes that COPY, but lines are matched by TEXT: if the other writer authored a line with the same text, one survives. A line you both edited appears TWICE, once each. And it merges BY LINE with no knowledge of markup, so a fenced code block you both edit can interleave — keep a note on its own line. Standing guidance that should outlive a session belongs in the `agent_instructions` property. |
| `properties` | object |  | property keys to write; each must be registered (see property_register) |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

```console
$ dot dot_set file=notes/retention.md 'title=Data retention policy' 'tags=["policy","compliance"]' 'body=Reviewed each quarter by the data protection lead.'
{"ok":true,"result":{"path":"notes/retention.md","title":"Data retention policy","tags":["policy","compliance"],"properties":{"kind":"prose","summary":"<!-- unread - summarize this file -->"},"body":"Reviewed each quarter by the data protection lead.\n"}}
```

`dot_set` also carries a `properties` bag, so a title, tags and metadata land in ONE write instead of a `dot_set` followed by a run of `property_set` calls. Same registry rule applies — every key in the bag must be registered first.

```console
$ dot dot_set file=notes/deletion-runbook.md 'title=Deletion runbook' 'properties={"owner":"bob"}'
{"ok":true,"result":{"path":"notes/deletion-runbook.md","title":"Deletion runbook","tags":[],"properties":{"kind":"prose","summary":"<!-- unread - summarize this file -->","owner":"bob"},"body":""}}
```

---

## Files

## `file_create`

Create or overwrite a FILE, and mint its DOT in the same act. Two artifacts, and the split is the whole model: the file at `path` holds the content and stays RAW — no frontmatter is ever written into it — while `title`, `tags` and `properties` land on the dot beside it at `.dotworld/dots/<path>.dot.md`. A file committed without its dot is invisible to every clone, which is why this writes both. REFUSES a `content` write to a path whose bytes on disk are BINARY: `content` is a string, so the write would replace an image or archive with its utf8 decode. Delete such a file first to replace it with text on purpose, or change its bytes yourself and stage them with `git`. The answer is addressed by `path` and states it ONCE (#420): there is no row-level `id`. The nested `dot` is the sidecar's own frontmatter read back verbatim, `id:` line and all — a record on disk, not a second handle, and a hand-moved or hand-edited `.dot.md` makes it disagree with `path`. Address files by `path`. DOTWORLD NEVER COMMITS: this writes the file and its dot to disk and nothing more — staging and committing are yours, with `git` (subcommand=add, then subcommand=commit) or your own shell. `workspace_submit` refuses to land a workspace with uncommitted changes, naming them, rather than silently reporting success over work still sitting on disk.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `path` | string | yes | source-relative path |
| `content` | string |  | source file content |
| `title` | string |  | title (else derived from H1/filename). `dot_set` changes it later |
| `tags` | array of string |  | tags — must be strings. `dot_set` changes them later |
| `properties` | object |  | dot properties. Each key must be REGISTERED (see `property_register`) and may not be reserved dot frontmatter — use the `title`/`tags` params above, not the bag, which would overwrite the dot's real field. A refused write leaves NO file behind. |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

Creates the file and mints its dot in one act — no `sync_source` needed afterwards.

```console
$ dot file_create path=notes/incident-2026-03.md 'content=# Incident: purge job skipped a night

The nightly purge did not run on 2026-03-11. Logs older than 90 days
survived one extra day. No customer data left the system.
' 'title=Incident: purge job skipped a night' 'tags=["incident"]'
{"ok":true,"result":{"sourceId":"handbook","path":"notes/incident-2026-03.md","title":"Incident: purge job skipped a night","tags":["incident"],"dot":{"id":"notes/incident-2026-03.md","source_id":"handbook","title":"Incident: purge job skipped a night","tags":["incident"],"source_path":"$DOTWORLD_ROOT/notes/incident-2026-03.md","properties":{"kind":"prose","summary":"<!-- unread - summarize this file -->"},"body":""}}}
```

---

## `file_delete`

Remove a file and its dot as one act — the inverse of `file_create`. Exactly two things are deleted, and that pairing is the unit: a file removed without its `.dot.md` leaves an orphan, durable metadata describing nothing, which is the case `dot_list orphaned=true` exists to make findable — so this never creates one. Identity is the path, which is why there is no separate retire verb: naming a path whose file is already gone retires its stranded dot, and that is how you clear an orphan you decided not to reattach. The dot goes with the file, and there is no separate confirmation: whatever it held — tags, properties, a body, a hand-written title — is deleted along with the file it describes; naming the file is the instruction for both. References are reported, not removed, and not a refusal: authored connectors naming this path are kept on purpose (an edge outliving its endpoint is how provenance works, so a `supersedes` edge pointing here still answers `graph_provenance` — use `graph_disconnect` if you want one gone), and `referencedBy` names the files whose own text links here, which dotworld does not edit because a wikilink is bytes in somebody's document. Refused on a tree with unmerged paths, exactly as `file_create` is — and refused when git does not hold the file's current bytes: untracked, ignored, or modified since the last commit, because then this is the only copy and nothing could bring it back. Commit it first and the delete is recoverable from history, or pass force=true — that gate covers the file, and the dot rides along with whatever it decides. dotworld never commits: this removes the file and its dot from disk and nothing more — staging and committing are yours, with `git` or your own shell. `workspace_submit` refuses to land a workspace with uncommitted changes, naming them, rather than silently reporting success over work still sitting on disk.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `path` | string | yes | source-relative path of the file to delete |
| `force` | boolean |  | delete the file even though git does not hold its current bytes (default false). This is the unrecoverable case and the only one, so the refusal names which of untracked, ignored or modified it hit; a clean tracked file never needs this |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

Removes the file and its dot as one act — the inverse of `file_create`.

```console
$ dot file_delete path=notes/incident-2026-03.md force=true
{"ok":true,"result":{"path":"notes/incident-2026-03.md","removed":true,"dotRemoved":true,"connectors":[],"referencedBy":["notes/retention-pack.md"],"detail":"notes/retention-pack.md still links to notes/incident-2026-03.md in its own text. That link is now dead, and dotworld did not edit it: a wikilink is bytes in somebody's document, so removing one is an authored change (`file_create`)."}}
```

---

## `file_read`

Read the contents of one or more files. A row with `content` is the file byte for byte and is authoritative. Two kinds of row carry no body. `binary: true` means the file is not text (a NUL byte in its first 8000 — git's own test), so `content` is omitted rather than utf8-decoded: the decode would give you U+FFFD where the bytes were, and writing that back destroys the file, so `file_create`/`file_stage` refuse such a path. Read what dotworld knows about it with `dot_get` instead, and change its bytes with `git`. `dotUnparseable: true` is the opposite case — the file itself read fine and only its dot could not be parsed.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `files` | array of string |  | file paths or ids. Over the CLI this is CWD-relative, exactly like `git add` — MCP and HTTP always take it relative to the source root (#337). |
| `index` | string |  | a document whose LINKS are the list of files — use instead of (or as well as) `files`. Any document works, not a special format: a generated index from `index_create`, or a hand-written note with links in prose. Read from the file every time, so pruning a line changes the answer. External URLs and dead links are skipped rather than reported missing. Over the CLI this is CWD-RELATIVE, exactly like git (#337); MCP and HTTP always take it relative to the source root. |
| `ranges` | object |  | optional { file: [start,end] } line ranges |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot file_read 'files=["notes/retention.md"]'
# Data retention policy

We keep customer event logs for 90 days, then delete them. Billing records are
kept for seven years, because the tax authority can ask for them that far back.

Deletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].
```

An index IS a document — nothing more. Read it like any other file and you see the list of wikilinks `index_create` wrote. Prune a line and the next `index=` call loads one file fewer; there is no snapshot.

```console
$ dot file_read 'files=["notes/retention-pack.md"]'
# Retention pack

- [Data retention policy](retention.md)
- [Deletion runbook](deletion-runbook.md)
- [Incident: purge job skipped a night](incident-2026-03.md)
```

The same document used as a NAME for that set of files: `index=` reads the links out of it and returns the files they point at.

```console
$ dot file_read index=notes/retention-pack.md
==> notes/retention.md <==
# Data retention policy

We keep customer event logs for 90 days, then delete them. Billing records are
kept for seven years, because the tax authority can ask for them that far back.

Deletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].

==> notes/deletion-runbook.md <==
# Deletion runbook

Run the purge job nightly. It reads the retention window from config, never
from a constant in the code.

==> notes/incident-2026-03.md <==
# Incident: purge job skipped a night

The nightly purge did not run on 2026-03-11. Logs older than 90 days
survived one extra day. No customer data left the system.
```

The conflicted file, markers and all. Read it before you settle it.

```console
$ dot file_read 'files=["notes/retention.md"]'
# Data retention policy

<<<<<<< HEAD
We keep customer event logs for 180 days, then delete them. Billing records are
=======
We keep customer event logs for 30 days, then delete them. Billing records are
>>>>>>> b1e047b (retention: shorten the log window to 30 days)
kept for seven years, because the tax authority can ask for them that far back.

Deletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].

ANSWERED FROM THE WORKSPACE /path/to/state/dotworld/worktrees/handbook/bob — resolved from your current directory, not from anything you passed. Pass `workspace=<path>` (see `workspace_list`) to choose a different tree explicitly.
```

---

## `file_stage`

Finish a conflicted file: write your reconciled text for ONE path that git currently reports as unmerged, and stage it. Does NOT commit — you commit with the `git` command, which is what keeps `rebase --continue` / `merge --continue` working. This is the step between reading a conflicted file (`file_read`) and continuing the rebase: without it you can see a collision and not answer it. NOT a general file write and NOT how you author: it refuses any path git does not report as unmerged, and it never touches the file's dot. Use `file_create` to author (it materialises the dot; a file landed without its .dot.md is invisible to every clone). Content still containing conflict markers is refused — send the reconciled text, not the marked block. So is a path whose bytes on disk are BINARY: this command writes a string, and a conflicted image is settled with `git checkout --ours`/`--theirs` and `git add`, not with reconciled text.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `path` | string | yes | the unmerged source-relative path |
| `content` | string | yes | the reconciled file content, markers removed |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

Write the reconciled text for ONE conflicted path. This is how a conflict ends — dotworld never picks a side, and never hands you a per-field payload to answer.

```console
$ dot file_stage path=notes/retention.md 'content=# Data retention policy

We keep customer event logs for 90 days, then delete them. Billing records are
kept for seven years, because the tax authority can ask for them that far back.

Deletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].
'
{"ok":true,"result":{"path":"notes/retention.md","tree":"/path/to/state/dotworld/worktrees/handbook/bob","staged":true,"committed":false}}
```

---

## `file_tree`

List what is in a source, as a directory tree. Each row is `{ name, path, type }`, plus `title` on a file and `children` on a directory. Identity is the path, so there is no separate id to carry — address a row anywhere else in the command set by its `path`. `name` is that path's last segment. `type` is stated rather than inferred: a directory cut off by `depth`, and an empty one, both carry an empty `children`.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `root` | string |  | subtree root path (default whole source) |
| `depth` | number |  | levels of PATH SEGMENTS to expand below the root, inclusive (depth=1 shows the root's children AND theirs). Default 2 — bounded, so an unparameterised call cannot return a whole large source; pass a negative depth for no limit. A dir cut off by the limit is still returned, flagged `truncated`, with `hiddenFiles` and `hiddenEntries` counting what lies beneath it |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot file_tree root=. depth=2
decisions/
  2026-02-adopt-postgres.md	Decision: adopt Postgres
notes/
  deletion-runbook.md	Deletion runbook
  onboarding.md	Onboarding a new engineer
  retention.md	Data retention policy
```

---

## Search

## `search_grep`

Exact-string / identifier lookup — grep over files, always current. Binary files are skipped, as git's own grep skips them: they have no lines, and matching their utf8 decode returned a "line" of replacement characters from inside an image.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `pattern` | string | yes | regex pattern |
| `flags` | string |  | regex flags |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot search_grep 'pattern=90 days'
notes/retention.md:3:We keep customer event logs for 90 days, then delete them. Billing records are
```

---

## `search_properties`

Find files by dot metadata — e.g.: which files have owner=alice, which are over five hours, which are still untagged. Every criterion is ANDed with the others; there is no `or`. `where` takes exact values (`{"owner":"alice"}`, matched by value and never by stringification — 3 is not "3") and comparisons, written as an operator object: `{"hours":{"gt":5},"status":{"ne":"done"}}`, with `eq` `ne` `gt` `gte` `lt` `lte`. Ordering compares numbers numerically and strings lexicographically, which is what sorts ISO dates; a comparison across types, or against a boolean/array/object/null, is refused rather than silently dropped. A key the file does not carry matches `ne` and nothing else. A `where` value is read as operators only when all its keys are operator names, so a property whose real value is literally {"gt":5} cannot be queried. `hasKey`/`missingKey` ask whether a property is there at all; `hasTag`/`missingTag` ask the same of tags, which are frontmatter and not properties, so they never match a property criterion. `files` narrows the scan to a candidate set — feed it a `grep -rl` result — and on its own returns those files, which is the one way to get an answer with no property criteria at all. An empty `files` answers `[]`: a grep that matched nothing leaves no candidates, so it cannot leave matches, and reading it as "no scope" would answer with the whole source. A file carrying dotUnparseable: true is returned regardless of the criteria: its metadata cannot be read, so whether it matches is unknown rather than false (`files` still bounds it — that is a scope, not a criterion). An unregistered key still matches — the data is the data — and the advisory beside the result says so, distinguishing a key that is on files but undeclared from one that is simply absent. Every row is addressed by `path` and states it ONCE (#420): there is no row-level `id`. The nested `dot` is the sidecar's own frontmatter read back verbatim, `id:` line and all — a record on disk, not a second handle, and a hand-moved or hand-edited `.dot.md` makes it disagree with `path`. Address files by `path`.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `where` | object |  | property criteria, ANDed. A plain value means equality; an operator object — every key one of eq/ne/gt/gte/lt/lte — means a comparison |
| `hasKey` | string |  | match files carrying this property key, whatever its value |
| `missingKey` | string |  | match files NOT carrying this property key |
| `hasTag` | string |  | match files carrying this tag |
| `missingTag` | string |  | match files NOT carrying this tag |
| `files` | array of string |  | restrict the scan to these file paths (a candidate set, e.g. from `grep -rl`); alone, it returns their dot metadata. An unindexed path is skipped, but a list where NONE resolve is refused; an empty list answers `[]` |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot search_properties 'where={"owner":"alice"}'
{"ok":true,"result":[{"sourceId":"handbook","path":"notes/retention.md","title":"Data retention policy","tags":["policy","compliance"],"dot":{"id":"notes/retention.md","source_id":"handbook","title":"Data retention policy","tags":["policy","compliance"],"source_path":"$DOTWORLD_ROOT/notes/retention.md","properties":{"kind":"prose","summary":"<!-- unread - summarize this file -->","owner":"alice"},"body":"Reviewed each quarter by the data protection lead.\n"}}]}
```

```console
$ dot search_properties hasKey=owner
{"ok":true,"result":[{"sourceId":"handbook","path":"notes/deletion-runbook.md","title":"Deletion runbook","tags":[],"dot":{"id":"notes/deletion-runbook.md","source_id":"handbook","title":"Deletion runbook","tags":[],"source_path":"$DOTWORLD_ROOT/notes/deletion-runbook.md","properties":{"kind":"prose","summary":"<!-- unread - summarize this file -->","owner":"bob"},"body":""}},{"sourceId":"handbook","path":"notes/retention.md","title":"Data retention policy","tags":["policy","compliance"],"dot":{"id":"notes/retention.md","source_id":"handbook","title":"Data retention policy","tags":["policy","compliance"],"source_path":"$DOTWORLD_ROOT/notes/retention.md","properties":{"kind":"prose","summary":"<!-- unread - summarize this file -->","owner":"alice"},"body":"Reviewed each quarter by the data protection lead.\n"}}]}
```

---

## `search_semantic`

Search the source by meaning: describe the task or question and get back the files most likely to answer it, ranked. A hit carrying dotUnparseable: true has unreadable metadata — its summary/summaryStatus are placeholders, not facts; do not read "unread" as "nobody summarised it". `obsolete`/`superseded` are stated on every hit — check them before reading: a superseded file can still rank above its replacement, and this is the only reliable way to tell. The top of the ranking is reranked with lexical query/document overlap before `limit` is applied, which corrects a known failure mode where whole-document embedding leaves several candidates nearly tied — this narrows near-ties, it does not override a decisive semantic match. Refuses with BAD_PARAMS, rather than silently ranking on a weaker fallback, when dotworld.config.json names a specific embedding engine that could not be built — engine: "auto" degrades instead of refusing. A long document is embedded in chunks and scored on its best-matching one, so a match late in a file ranks it: `maxTokens` in embedding_info is the per-chunk ceiling, not a limit on how much of a file was read.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `query` | string | yes | the task / query text |
| `limit` | number |  | max results (default 10) |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

Ranked candidates, not answers. `score` is cosine similarity after the lexical rerank.

```console
$ dot search_semantic 'query=how long do we keep customer logs' limit=3
?0.71	notes/retention.md	Data retention policy
?0.53	notes/onboarding.md	Onboarding a new engineer
?0.47	notes/deletion-runbook.md	Deletion runbook
```

---

## `search_triage`

Ranked 'important but going stale' list, computed from the files of ONE working tree. Its link features — `inLinks`, `linkWeightSum` and `centrality` alike — count no `supersedes` connector: recording that a document is dead used to rank it UP, and it compounds, because the superseded file is by construction the older of the pair. On a corpus with no differentiating signal yet — one batch commit, no links — every file scores identically and the order is arbitrary; an advisory beside the result says so — do not read a flat list as a ranking.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

Ranks "important but going stale". The score is time-derived, so it is redacted here — what the example shows is which signals go into it.

```console
$ dot search_triage
{"ok":true,"result":[{"path":"notes/deletion-runbook.md","recencyDays":"<varies>","edits":1,"inLinks":2,"linkWeightSum":2,"centrality":0.3255080588644246,"humanEdits":1,"machineEdits":0,"importance":"<varies>","staleness":"<varies>","score":"<varies>"},{"path":"notes/retention.md","recencyDays":"<varies>","edits":1,"inLinks":2,"linkWeightSum":2,"centrality":0.2242466713385889,"humanEdits":1,"machineEdits":0,"importance":"<varies>","staleness":"<varies>","score":"<varies>"},{"path":"notes/incident-2026-03.md","recencyDays":null,"edits":0,"inLinks":1,"linkWeightSum":1,"centrality":0.1348983882271151,"humanEdits":0,"machineEdits":0,"importance":"<varies>","staleness":"<varies>","score":"<varies>"},{"path":"decisions/2026-02-adopt-postgres.md","recencyDays":"<varies>","edits":1,"inLinks":0,"linkWeightSum":0,"centrality":0.1051156271899572,"humanEdits":1,"machineEdits":0,"importance":"<varies>","staleness":"<varies>","score":"<varies>"},{"path":"notes/onboarding.md","recencyDays":"<varies>","edits":1,"inLinks":0,"linkWeightSum":0,"centrality":0.1051156271899572,"humanEdits":1,"machineEdits":0,"importance":"<varies>","staleness":"<varies>","score":"<varies>"},{"path":"notes/retention-pack.md","recencyDays":null,"edits":0,"inLinks":0,"linkWeightSum":0,"centrality":0.1051156271899572,"humanEdits":0,"machineEdits":0,"importance":"<varies>","staleness":"<varies>","score":"<varies>"}]}
```

---

## Properties

## `property_get`

READ PROPERTIES off named files — the matching getter for `property_set`, and the context-cheapest way to ask "what is `owner` on these five files?". Name the files with `files`, or point `index` at a document and every file IT LINKS TO is the list; name the keys with `keys`, or omit it for the whole property bag. A requested key that is NOT on the dot is listed in `missing` rather than answered as null — "the value is nothing" and "the key is absent" are different facts, and reading one as the other is how a caller concludes an earlier write failed. THIS IS WHY TO PREFER IT OVER `dot_get fields=[…]`: that projection simply omits a key the dot does not carry, so `dot_get fields=["owner"]` on a file with no owner answers `{"path":"a.md"}` and you cannot tell an absent property from one you forgot to ask for. `property_list` is what tells you which keys EXIST to ask for.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `files` | array of string |  | file paths or ids. Over the CLI this is CWD-relative, exactly like `git add` — MCP and HTTP always take it relative to the source root (#337). |
| `index` | string |  | a document whose LINKS are the list of files — use instead of (or as well as) `files`. Any document works, not a special format: a generated index from `index_create`, or a hand-written note with links in prose. Read from the file every time, so pruning a line changes the answer. External URLs and dead links are skipped rather than reported missing. Over the CLI this is CWD-RELATIVE, exactly like git (#337); MCP and HTTP always take it relative to the source root. |
| `keys` | array of string |  | property keys to read (default: every property on the dot) |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot property_get 'files=["notes/retention.md"]' 'keys=["owner"]'
{"ok":true,"result":[{"path":"notes/retention.md","properties":{"owner":"alice"}}]}
```

---

## `property_list`

This source's property vocabulary — what it declares and what is actually in use, in one answer. Read this before `property_set`: each row gives the key, `origin`, `registered`, the declared `merge` and `description`, how many files carry it, the value types, and the `values` actually in use — which is what stops you inventing a synonym for a term the source already has. `origin` separates two populations that used to look identical: `source` means declared in this source's `.dotworld/properties.json`, so it is in git and travels with a clone and can be withdrawn by `property_unregister`; `built-in` means it ships with dotworld, is deliberately not in that file, and cannot. `values` is capped and `distinctValues` always carries the true total beside it — when they differ you are looking at a sample, and long or structured values are counted but not listed. A registered property appears even at count 0, so you can read what a source allows before anyone has used it; an unregistered property in use appears with registered:false and no origin — real metadata, but nothing merges it and no write can target it until you `property_register` it. Pass `file` or `index` to scope the counts and values to those files ("what is on this") while still listing the whole declared vocabulary ("what may I write here"). Files whose dot cannot be parsed contribute nothing to these counts — the advisory beside the result names them.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `file` | string |  | scope the counts and values to this one file (the vocabulary rows still all appear) |
| `index` | string |  | scope to every file THIS DOCUMENT LINKS TO — same expansion `dot_get index=` uses. Combines with `file`. |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot property_list
{"ok":true,"result":[{"key":"agent_instructions","count":0,"types":[],"registered":true,"origin":"built-in","merge":"last-writer","description":"Standing guidance for anyone about to work on this file — a per-file CLAUDE.md. \"The tests here are flaky under load, do not trust a green run\"; \"this API is frozen, propose changes in the ADR\". Read it before editing the file. Advisory: nothing in the kernel reads or enforces it, so it is worth only what the next reader does with it. It is the STABLE half of what a dot says about a file; in-flight notes and open questions belong in the dot body, which merges by line-union so two writers can both leave one.","values":[],"distinctValues":0},{"key":"kind","count":4,"types":["string"],"registered":true,"origin":"built-in","merge":"last-writer","description":"The file's kind (prose/code/config/…), derived from its path by the source's kind rules.","values":["prose"],"distinctValues":1},{"key":"obsolete","count":0,"types":[],"registered":true,"origin":"built-in","merge":"last-writer","description":"Marks a file superseded by another. Written by the obsolescence plugin as a file correction.","values":[],"distinctValues":0},{"key":"owner","count":2,"types":["string"],"registered":true,"origin":"source","merge":"last-writer","description":"the person accountable for this file","values":["alice","bob"],"distinctValues":2},{"key":"render-hint","count":0,"types":[],"registered":true,"origin":"built-in","merge":"last-writer","description":"How a viewer should present this file. Advisory; nothing in the kernel reads it.","values":[],"distinctValues":0},{"key":"summary","count":4,"types":["string"],"registered":true,"origin":"built-in","merge":"last-writer","description":"A short (~100 word) description of what the source file contains, so a reader can decide whether to open the file itself. Written by whoever last read the file in full.","values":["<!-- unread - summarize this file -->"],"distinctValues":1},{"key":"summary_blob","count":0,"types":[],"registered":true,"origin":"built-in","merge":"last-writer","description":"The git blob SHA of the source body the summary was written against - THE staleness stamp. Asked of `git hash-object`, never computed here, so the repo's own clean filters decide what counts as a change. Maintained by dotworld; do not set it by hand.","values":[],"distinctValues":0},{"key":"summary_hash","count":0,"types":[],"registered":true,"origin":"built-in","merge":"last-writer","description":"Legacy, no longer written or read: the content hash dotworld once computed for the source body a summary was written against. Superseded by `summary_blob`, which is git's own answer to the same question. Dropped when the summary is next rewritten.","values":[],"distinctValues":0}]}
```

---

## `property_register`

Declare a property in this source's registry (`.dotworld/properties.json`, durable and in git), so it can be written, targeted by commands, and merged by a strategy you chose. The registry is per source: registering here licenses nothing in another source. Registering an existing name updates it — that is how a source changes its mind — and the answer discloses what changed: when the merge strategy differs from the one that governed before, it carries `strategyChanged` (from → to) and `filesCarrying`, the number of dots already holding values the new strategy now governs (check those when changing shape: a scalar under `union` falls back to `last-writer`). Registering a name that ships with dotworld answers `overridesBuiltIn: true` — the override shadows the shipped declaration until `property_unregister` reverts it. An unchanged re-registration adds none of these fields. `merge` says how two writers editing the same property are reconciled: `last-writer` (scalars — a true divergence still conflicts), `union` (sets, with 3-way deletion tracking, so a removed element stays removed; a value that is not an array is not a set, so it falls back to `last-writer` the same way `line-union` does below), `line-union` (multi-line text — both writers' lines are kept, with the same deletion tracking; this is how the dot body merges. It never conflicts on text, but a value that is not a string has no lines, so it falls back to `last-writer` and a true divergence on it still conflicts — do not read it as "this property can never block a land"), `counter` (one number, merged by summing each side's own change against the merge base: a branch that left main at 5 and counted to 8 contributes +3, whichever order branches land in. Not a max and not a last-writer — those both answer 8 where 26 things happened. The value is a plain number, so `value=26` is the whole of it; a value that is not a finite number cannot be subtracted, so it falls back to `last-writer` like the two above, and that includes the per-actor `{actor: {count, lastReadAt}}` map that was this strategy's earlier internal shape — sum the actors and write the total. Two limits worth knowing: a merge resolved from plain 2-way conflict markers has no trustworthy base, so it conflicts rather than inventing a total, and deleting the property on one side while the other counts on resurrects it at that side's delta). An unregistered property is never deleted or migrated — it stays on the file — but nothing merges it: a divergence on it conflicts and stays conflicted until someone registers it or settles the line by hand. `description` is required and is what tells the next reader (often a model) what the property is for.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `name` | string | yes | property name (may not be reserved dot frontmatter: id, source_id, title, tags, source_path) |
| `merge` | string | yes | merge strategy: last-writer \| union \| line-union \| counter |
| `description` | string | yes | what this property is for — served to agents as its meaning |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

A property is registered per source or it has no features. The registry is `.dotworld/properties.json` — durable and in git.

```console
$ dot property_register name=owner merge=last-writer 'description=the person accountable for this file'
{"ok":true,"result":{"name":"owner","merge":"last-writer","description":"the person accountable for this file"}}
```

---

## `property_set`

Set ONE dot property (metadata never touches the source file). `property_get` is the matching read. The key must be REGISTERED IN THIS WORKING TREE — `property_list` shows the vocabulary, `property_register` extends it. An unregistered key is refused with BAD_PARAMS, and the refusal distinguishes "already on this file but unregistered" from "not registered in this working tree": the first means the metadata exists and registering it adopts what is written; the second checks only THIS working tree's copy of the registry — another branch or workspace of the same source may already declare it, in which case pulling that change in (rather than re-registering here) is what resolves it. RESERVED DOT FRONTMATTER IS ALSO REFUSED — `title`, `tags`, `id`, `source_id`, `source_path` are not properties and do not live in the property bag, so writing one here would overwrite the dot's real field instead of storing a property (`key=tags value=5` used to DELETE a file's tags and report success). Use `dot_set` for `title` and `tags`; the identity fields are dotworld's and nothing may set them.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `file` | string | yes | file path |
| `key` | string | yes | property key. Must be registered — see `property_register`, and `property_list` for what this source already declares. May NOT be reserved dot frontmatter (title, tags, id, source_id, source_path): those are not properties and `property_register` refuses them too — `dot_set` is what writes `title` and `tags`. |
| `value` | any |  | property value (any JSON value: string, number, boolean, object, array, or null) |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

```console
$ dot property_set file=notes/retention.md key=owner value=alice
{"ok":true,"result":{"ok":true}}
```

The refusal an unregistered property earns. Register it first, or the merge cannot know how to resolve two writers.

```console
$ dot property_set file=notes/retention.md key=reviewer value=bob
{"ok":false,"error":"`reviewer` is not registered in this working tree (source 'handbook'), and is on no dot there either. This only checks the registry on THIS working tree — another branch or workspace of the same source may already declare it; `property_list` there would show. Otherwise register it here first — `property_register name=reviewer merge=last-writer description='…'` — then set the value. `property_list` shows the vocabulary this working tree declares.","code":"BAD_PARAMS"}
```

---

## `property_unregister`

Withdraw a declaration from THIS source's registry — the counterpart to `property_register`. THE DATA STAYS: the property remains on every dot that carries it, because an unregistered property is never deleted or migrated. What it loses is its merge strategy and the ability to be targeted by a write, so `filesCarrying` in the answer tells you how much metadata just became unmergeable — learn it here rather than at a conflict. `property_unset` is what removes a property from a file. A BUILT-IN is refused: it ships with dotworld rather than living in `.dotworld/properties.json`, so there is nothing to take out (`property_list` marks each row `built-in` or `source`). Withdrawing a source OVERRIDE of a built-in works and reverts it to the shipped declaration.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `name` | string | yes | property name to withdraw (must be declared by this source) |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

Withdraws the declaration. Values already written to dots stay on the files — this is the vocabulary, not the data.

```console
$ dot property_unregister name=owner
{"ok":true,"result":{"name":"owner","removed":{"merge":"last-writer","description":"the person accountable for this file"},"revertedToBuiltIn":false,"filesCarrying":1}}
```

---

## `property_unset`

Remove a property from ONE dot. The source file is never touched, and no other property is. Removing a key that is not there is not an error — `removed: false` says the requested end state already held. AN UNREGISTERED KEY IS REMOVED TOO, by explicit request. The registry forbids the MERGE from deleting or migrating an unregistered property, because dotworld silently discarding metadata it has no strategy for is what the registry exists to prevent — but this is a caller naming a key, not an auto-resolution, and refusing would make `property_register` a required step towards deleting something. Reserved dot frontmatter (title, tags, …) is refused: it is not a property — use `dot_set`. Unsetting `summary` drops its staleness stamp (`summary_blob`) with it.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `file` | string | yes | file path |
| `key` | string | yes | property key to remove |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

```console
$ dot property_unset file=notes/retention.md key=owner
{"ok":true,"result":{"path":"notes/retention.md","key":"owner","removed":true,"wasRegistered":true}}
```

---

## Indexes and context

## `context_load`

Join a list of files into one prompt-ready blob of context. Because the list can be an index document, this works as a dynamic alternative to a static CLAUDE.md or agent-definition file: point `index` at a document and edit that document to change what loads next time, with nothing to redeploy. `mode=file` joins full file bodies (`file_read`); `mode=dot` joins each dot (`dot_get`) — title, tags, summary, authored body. Same resolution as those two commands: `files`, `index`, or both (index appended after files, deduped) — naming neither refuses. `entries` carries the raw per-file rows (exactly what `file_read`/`dot_get` would answer) for a caller that wants to filter or reorder; `context` is the same information already joined into one string, in resolution order, ready to drop into a prompt. `missing` names anything that did not resolve (not found, or a dot that could not be parsed) — never a refusal. `mode=file` also reports `binary`: file paths whose bytes are not text, skipped from the blob rather than lossily decoded, exactly like `file_read`. Adds nothing `file_read`/`dot_get` do not already compute — a formatting convenience over their output, never a third source of truth.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL. Omitted, dotworld infers it from your working directory (your own workspace counts), exactly like every other command's sourceId. |
| `files` | array of string |  | file paths or ids to load — use instead of (or as well as) `index`. Over the CLI this is CWD-relative, exactly like `git add`; MCP and HTTP always take it relative to the source root. |
| `index` | string |  | a document whose LINKS are the list of files to load — use instead of (or as well as) `files`. Any document works: a generated index from `index_create`, or a hand-written note with links in prose. Read from the file every call, so pruning a line changes the answer, exactly like `file_read index=`/`dot_get index=`. |
| `mode` | string | yes | 'file' loads full file bodies (what `file_read` returns) — the raw content, byte for byte. 'dot' loads each file's dot (what `dot_get` returns) — title, tags, summary and authored body, without the file body itself. Pick `file` when the files themselves are the context (source, specs); pick `dot` when a lighter summary of many files is enough. No default — the two answers differ enough that guessing one would silently pick a context budget for you. |
| `workspace` | string |  | The working tree to answer from (default: the canonical checkout — see `file_read`'s `workspace` param, which this matches exactly). |

`mode=dot` joins each file’s dot — title, tags, summary, authored body. The light option when a summary of many files is enough.

```console
$ dot context_load index=notes/retention-pack.md mode=dot
{"ok":true,"result":{"mode":"dot","entries":[{"path":"notes/retention.md","title":"Data retention policy","tags":["policy","compliance"],"summary":"<!-- unread - summarize this file -->","centrality":0.2242466713385889,"obsolete":false,"kind":"prose","owner":"alice","body":"Reviewed each quarter by the data protection lead.\n","summaryStatus":"unread"},{"path":"notes/deletion-runbook.md","title":"Deletion runbook","tags":[],"summary":"<!-- unread - summarize this file -->","centrality":0.3255080588644246,"obsolete":false,"kind":"prose","owner":"bob","body":"","summaryStatus":"unread"},{"path":"notes/incident-2026-03.md","title":"Incident: purge job skipped a night","tags":["incident"],"summary":"<!-- unread - summarize this file -->","centrality":0.1348983882271151,"obsolete":false,"kind":"prose","body":"","summaryStatus":"unread"}],"context":"## Data retention policy (notes/retention.md)\ntags: policy, compliance\n\n<!-- unread - summarize this file -->\n\nReviewed each quarter by the data protection lead.\n\n\n---\n\n## Deletion runbook (notes/deletion-runbook.md)\n\n<!-- unread - summarize this file -->\n\n---\n\n## Incident: purge job skipped a night (notes/incident-2026-03.md)\ntags: incident\n\n<!-- unread - summarize this file -->","missing":[]}}
```

`mode=file` joins the full file bodies instead. Same list, a much larger context budget — which is why there is no default.

```console
$ dot context_load index=notes/retention-pack.md mode=file
{"ok":true,"result":{"mode":"file","entries":[{"path":"notes/retention.md","content":"# Data retention policy\n\nWe keep customer event logs for 90 days, then delete them. Billing records are\nkept for seven years, because the tax authority can ask for them that far back.\n\nDeletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].\n"},{"path":"notes/deletion-runbook.md","content":"# Deletion runbook\n\nRun the purge job nightly. It reads the retention window from config, never\nfrom a constant in the code.\n"},{"path":"notes/incident-2026-03.md","content":"# Incident: purge job skipped a night\n\nThe nightly purge did not run on 2026-03-11. Logs older than 90 days\nsurvived one extra day. No customer data left the system.\n"}],"context":"## notes/retention.md\n\n# Data retention policy\n\nWe keep customer event logs for 90 days, then delete them. Billing records are\nkept for seven years, because the tax authority can ask for them that far back.\n\nDeletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].\n\n\n---\n\n## notes/deletion-runbook.md\n\n# Deletion runbook\n\nRun the purge job nightly. It reads the retention window from config, never\nfrom a constant in the code.\n\n\n---\n\n## notes/incident-2026-03.md\n\n# Incident: purge job skipped a night\n\nThe nightly purge did not run on 2026-03-11. Logs older than 90 days\nsurvived one extra day. No customer data left the system.\n","missing":[],"binary":[]}}
```

---

## `index_create`

Save a set of files as an index document — a list of links to them, so a search result or a hand-picked reading list becomes something durable you can come back to. The index is an ordinary file: in git, with a dot of its own, and its links become connectors, so it shows up in `graph_get` like anything else. It carries no summaries — those live on the dot, and a copy here would go stale on its own. Prune it by editing the file; `dot_get index=` and `file_read index=` read the file, so your edit is the answer. Refuses to replace an existing file unless overwrite=true. A path that is not a file is reported in `missing` and left out — one stale path never costs you the index.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `path` | string | yes | source-relative path to write the index at |
| `files` | array of string | yes | file paths or ids to list, in the order you want them |
| `title` | string |  | index title (default: derived from the filename) |
| `overwrite` | boolean |  | replace an existing file (default false) |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

Writes a document that is a list of wikilinks. Every command taking `files` also takes `index`, so this file becomes a reusable name for that set.

```console
$ dot index_create path=notes/retention-pack.md 'files=["notes/retention.md","notes/deletion-runbook.md","notes/incident-2026-03.md"]' 'title=Retention pack'
{"ok":true,"result":{"path":"notes/retention-pack.md","files":["notes/retention.md","notes/deletion-runbook.md","notes/incident-2026-03.md"],"missing":[]}}
```

---

## The graph

## `graph_affected`

List the files a change is likely to touch: the union of what the embedding finds similar to the seed and what the graph connects to it.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `seed` | string | yes | a file path/id or free text describing the change |
| `depth` | number |  | graph hops (default 1) |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

Impact, not similarity: the embedding neighbourhood unioned with the connector graph.

```console
$ dot graph_affected 'seed=retention window' depth=1
{"ok":true,"result":[{"path":"notes/deletion-runbook.md","title":"Deletion runbook","reason":"both","score":0.7099,"isFile":true},{"path":"notes/onboarding.md","title":"Onboarding a new engineer","reason":"both","score":0.6969,"isFile":true},{"path":"notes/retention.md","title":"Data retention policy","reason":"both","score":0.6908,"isFile":true},{"path":"decisions/2026-02-adopt-postgres.md","title":"Decision: adopt Postgres","reason":"embedding","score":0.5101,"isFile":true}]}
```

---

## `graph_connect`

Author an explicit connector (incl. supersedes provenance edges). Authoring one over a pair the body text already links gives ONE edge carrying your type and weight, not two counted twice.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `from` | string | yes | from file path |
| `to` | string | yes | to file path |
| `type` | string |  | connector type (default links) |
| `weight` | number |  | weight (default 1). A PROPORTION, not an amount: it says how this file splits its own rank across its out-edges, so scaling every out-edge of one file changes nothing. Must be finite and greater than 0 — it divides into PageRank centrality and sums into `search_triage` ranking, so a non-finite weight would NaN those scores for the whole source. A small fraction says this edge counts for little; `graph_disconnect`, not `weight=0`, says it counts for nothing. |
| `properties` | object |  | connector properties |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

```console
$ dot graph_connect from=notes/incident-2026-03.md to=notes/deletion-runbook.md type=links
{"ok":true,"result":{"id":"links:notes/incident-2026-03.md->notes/deletion-runbook.md","sourceId":"handbook","from":"notes/incident-2026-03.md","to":"notes/deletion-runbook.md","type":"links","weight":1,"properties":{},"derived":false}}
```

`supersedes` is a provenance edge — it records that one file replaced another, which is what `graph_provenance` reads.

```console
$ dot graph_connect from=notes/retention.md to=decisions/2026-02-adopt-postgres.md type=supersedes
{"ok":true,"result":{"id":"supersedes:notes/retention.md->decisions/2026-02-adopt-postgres.md","sourceId":"handbook","from":"notes/retention.md","to":"decisions/2026-02-adopt-postgres.md","type":"supersedes","weight":1,"properties":{},"derived":false}}
```

---

## `graph_disconnect`

Remove an AUTHORED connector — the inverse of `graph_connect`. Name the same `from`, `to` and `type` you connected with (`type` defaults to 'links', so a `supersedes` edge must be named as one). Authored connectors are the only ones this touches: an edge that comes from a `[[wikilink]]` or a markdown link in a file's BODY is derived, recomputed from the text on every read, and is refused here — removing it means editing that document with `file_create`, and nothing else would make it stick. When BOTH exist over the same pair, the authored one is removed and `derivedEdgeRemains: true` says the edge will still be in `graph_get`, so you are not left thinking the command did nothing.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `from` | string | yes | from file path |
| `to` | string | yes | to file path |
| `type` | string |  | connector type (default links) — must match the one you connected with |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

```console
$ dot graph_disconnect from=notes/incident-2026-03.md to=notes/deletion-runbook.md type=links
{"ok":true,"result":{"removed":true,"id":"links:notes/incident-2026-03.md->notes/deletion-runbook.md","from":"notes/incident-2026-03.md","to":"notes/deletion-runbook.md","type":"links","derivedEdgeRemains":false}}
```

---

## `graph_get`

Walk the links around one or more seed files and report what they connect to. Returns { seeds, nodes, edges }. Each node carries `hop` (0 = a seed) and `via` ({from, edgeType}) saying how it was reached, so the result reads as an ordered list rather than a topology you must re-derive; `superseded: true` marks a file something supersedes — do not read it as current. A `supersedes` edge is also the one type importance ignores, so an edge visible here contributes nothing to the `centrality` reported by `dot_get` and `search_triage`. `expanded` is true when ALL of that node's connectors (under the filters you asked for) are in this response, false when it sat on the cut, with `unexpandedEdges` counting what was not followed — so "no edges" never has to be guessed at. Re-query with that node as a seed to continue. Narrow with `direction`/`edgeTypes` before reaching for more hops: one filtered hop answers most questions.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `files` | array of string | yes | seed file paths or ids |
| `hops` | number |  | hops to expand from the seeds (default 1); 0 returns the seeds alone, unexpanded |
| `depth` | number |  | deprecated alias for `hops` — same meaning, kept for one release |
| `direction` | string |  | 'out' = what the seeds point at · 'in' = what points at them · 'both' (default). Any other value is refused with BAD_PARAMS rather than silently treated as 'both' |
| `edgeTypes` | array of string |  | restrict the walk to these connector types, e.g. ['supersedes'] to follow provenance only |
| `limit` | number |  | max nodes to return, nearest-first — LOSSY, so a set question ("everything that supersedes this") wants no limit; `complete` and `totalReachable` always say what you did not get |
| `exclude` | array of string |  | files to leave out of the result (ones you already have). Still walked THROUGH, so anything reachable only via an excluded file still comes back |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot graph_get 'files=["notes/retention.md"]' hops=1
{"ok":true,"result":{"seeds":{"resolved":["notes/retention.md"],"unresolved":[]},"nodes":[{"path":"notes/retention.md","title":"Data retention policy","centrality":0.28804982483429553,"isFile":true,"hop":0,"expanded":true},{"path":"notes/onboarding.md","title":"Onboarding a new engineer","centrality":0.15570260801935445,"isFile":true,"hop":1,"expanded":true,"via":{"from":"notes/retention.md","edgeType":"links"}},{"path":"notes/deletion-runbook.md","title":"Deletion runbook","centrality":0.4005449591269955,"isFile":true,"hop":1,"expanded":true,"via":{"from":"notes/retention.md","edgeType":"links"}}],"edges":[{"id":"derived:notes/onboarding.md->notes/retention.md","sourceId":"handbook","from":"notes/onboarding.md","to":"notes/retention.md","type":"links","weight":1,"properties":{},"derived":true},{"id":"derived:notes/retention.md->notes/deletion-runbook.md","sourceId":"handbook","from":"notes/retention.md","to":"notes/deletion-runbook.md","type":"links","weight":1,"properties":{},"derived":true}],"complete":true,"totalReachable":3,"originsOf":{}}}
```

---

## `graph_provenance`

Supersession (bi-temporal) edges touching a file — truth-history. REFUSES a `file` that is not in the source rather than answering the empty list, because `[]` for a path that is not there is indistinguishable from `[]` for a file nothing has ever superseded.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `file` | string | yes | file path or id |
| `workspace` | string |  | The working tree to answer from. THE INDEX IS PER WORKING TREE: this answer describes ONE tree, so two writers legitimately get different results, rankings, importance and read counts for the same query. OMITTING THIS DOES NOT MEAN "MY WORKSPACE": with no declared auth mode you are not routed to one, and the answer comes from the canonical checkout — where your own unlanded work is simply absent. Call `workspace_list` to see the workspaces that exist and pass one of their paths here; `workspace_open` creates yours if you have none. Whichever tree answers, an advisory beside the result names it and names any workspace it could not see. |

```console
$ dot graph_provenance file=decisions/2026-02-adopt-postgres.md
{"ok":true,"result":[{"id":"supersedes:notes/retention.md->decisions/2026-02-adopt-postgres.md","sourceId":"handbook","from":"notes/retention.md","to":"decisions/2026-02-adopt-postgres.md","type":"supersedes","weight":1,"properties":{},"derived":false,"direction":"incoming"}]}
```

---

## Workspaces and messaging

## `comms_read`

Read comms addressed to `as` (`to === as` or `to === 'all'`), oldest first. With `since` omitted (the common case) this resumes from a persisted per-reader cursor and ADVANCES it to the last message returned before returning — each message is delivered to a given `as` exactly once across calls, not re-scanned on every poll. Pass `since` explicitly to replay from an earlier point instead; an explicit `since` never touches the stored cursor. The response carries BOTH: `since` is what this call filtered from (your input echoed back on an explicit-`since` call, the advanced cursor on a default one), and `cursor` is the persisted pointer — where this `as` actually stands, and the point the next default call resumes from. Read `cursor`, not `since`, to tell a caught-up mailbox from a `since` that skipped a backlog nothing has consumed: both answer `messages: []`, and only `cursor` distinguishes them. To ABANDON a backlog without receiving it, pass `advance=true` (or `seek=<n>`): the cursor moves and NO messages are returned — the one deliberate hole in "a read is a consume". `since`, `seek` and `advance` are mutually exclusive; passing more than one is refused rather than ranked.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL. Omitted, dotworld infers it from your working directory (your own workspace counts), exactly like every other command's sourceId. |
| `as` | string | yes | the mailbox to read as — matches messages sent `to` this name or to 'all', and names the persisted cursor. |
| `since` | number |  | OPTIONAL — replay strictly after this sequence number instead of the stored cursor. Never ADVANCES the stored cursor: a high `since` skips nothing, it just filters this one response, and the next default read still replays the whole backlog. The response reports the untouched cursor as `cursor`. |
| `seek` | number |  | OPTIONAL — SKIP: set the persisted cursor to this sequence number and return NO messages, abandoning a backlog without transporting it. CLAMPED to the newest message in the log, because a cursor past the end would silently swallow FUTURE messages; the response reports where it actually landed as `cursor`. A value BELOW the current cursor REWINDS, and those messages are delivered again — the cursor is disposable derived state and the log is the truth, so a rewind loses nothing and is the only undo for a mis-seek. Mutually exclusive with `since` and `advance`. |
| `advance` | boolean |  | OPTIONAL — SKIP EVERYTHING: `true` sets the persisted cursor to the newest message in the log and returns NO messages. This is the spelling for "I do not know the newest sequence number and do not want to receive the backlog in order to find out"; `seek` is the same operation when you do know it. Messages sent AFTER the call are still delivered — this skips a backlog, it does not mute a mailbox. Only `true` is accepted. Mutually exclusive with `since` and `seek`. |

A default read resumes from a persisted per-reader cursor and ADVANCES it past everything it returns. Each message reaches a given `as` exactly once, so polling does not re-scan the log.

```console
$ dot comms_read as=bob
{"ok":true,"result":{"messages":[{"seq":1,"at":"<varies>","from":"alice","to":"bob","topic":"retention","body":"Incident note is in. The `purge` job needs a runbook line about the skipped night."}],"since":1,"cursor":1}}
```

Two more messages have been sent since. The cursor moved, so only the new ones come back — the first message is not repeated.

```console
$ dot comms_read as=bob
{"ok":true,"result":{"messages":[{"seq":2,"at":"<varies>","from":"alice","to":"bob","topic":"retention","body":"Runbook line landed."},{"seq":3,"at":"<varies>","from":"alice","to":"all","topic":"retention","body":"Window is settled at 90 days."}],"since":3,"cursor":3}}
```

Going BACKWARDS: an explicit `since` replays the log from an earlier point. `since=0` is the whole history. This is a replay, not a pointer move — the stored cursor is neither read nor advanced, and the `since` echoed back is your own input, not the cursor.

```console
$ dot comms_read as=bob since=0
{"ok":true,"result":{"messages":[{"seq":1,"at":"<varies>","from":"alice","to":"bob","topic":"retention","body":"Incident note is in. The `purge` job needs a runbook line about the skipped night."},{"seq":2,"at":"<varies>","from":"alice","to":"bob","topic":"retention","body":"Runbook line landed."},{"seq":3,"at":"<varies>","from":"alice","to":"all","topic":"retention","body":"Window is settled at 90 days."}],"since":0,"cursor":3}}
```

Proof of that: the very next default call still resumes from where it was, and there is nothing new. To learn the persisted cursor, call `comms_read` with `since` omitted and read the `since` it answers with. — **A default read is the ONLY thing that moves the pointer**, and it always hands back every unread message on the way. To skip a backlog you have decided not to read, run a default read and discard its output (`dot comms_read as=bob > /dev/null`); the cursor lands on the present. A high `since` does NOT do this: `since=99` answers `{ messages: [], since: 99 }`, which looks exactly like a caught-up mailbox, and the next default read still replays the whole backlog.

```console
$ dot comms_read as=bob
{"ok":true,"result":{"messages":[],"since":3,"cursor":3}}
```

---

## `comms_send`

Append one message to the source-wide comms log — cross-workspace messaging between sibling `workspace_open` agents on the same source. Not durable knowledge: this is scratch coordination, disposable like the rest of `.dotworld/derived/`, never a substitute for landing real work through `workspace_submit`. Every worktree of the source sees every message — storage is rooted at the source, not your own workspace.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL. Omitted, dotworld infers it from your working directory (your own workspace counts), exactly like every other command's sourceId. |
| `from` | string | yes | the sender's own name — there is no reliable identity to infer this from, so name yourself explicitly, the same string every time. |
| `to` | string | yes | the recipient's name, or 'all' to broadcast to every reader. |
| `topic` | string | yes | a short subject line. |
| `body` | string | yes | the message body. |

The body is data. It is passed as an argv element, never through a shell, so backticks in it stay backticks.

```console
$ dot comms_send from=alice to=bob topic=retention 'body=Incident note is in. The `purge` job needs a runbook line about the skipped night.'
{"ok":true,"result":{"ok":true,"seq":1,"at":"<varies>"}}
```

---

## `workspace_close`

Remove a workspace — the inverse of `workspace_open`. Your branch survives: the worktree is taken down through git (`worktree remove` then `prune`, never `rm -rf`), and `users/<name>` is deliberately left alone, so commits you have not landed are still there and `workspace_submit` still lands them — a land needs a branch, not a worktree. It refuses exactly one thing: uncommitted changes, which are on no branch, which `git worktree remove` deletes, and which nothing could bring back. `force=true` overrides that and discards them. Unlanded commits are not refused — they are not loss, the branch keeps them, `workspace_submit` still lands them with no worktree in existence, and `git subcommand=branch flags=--no-merged` finds the branch again — so they are reported: `unlanded` counts them and `detail` says what that count means. Unlanded relative to what: the branch the canonical checkout has checked out right now, which `detail` names. Read `detail` when `unlanded` is non-zero — the number measures ancestry, so a squash-land leaves it non-zero about work that is already in, and `detail` says so. A removal git declines (a locked worktree, a permission failure) is an error, never a success — this never reports `closed` for a directory still on disk. `workspace_list` shows every working tree, of which the `managed` ones are what this closes; reopening one recreates the worktree on the same branch, with its history intact.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `as` | string |  | close a workspace opened under a name (auth mode `none` ONLY, mirroring `workspace_open`; refused under a declared auth mode, where your workspace is routed by your authenticated identity and naming another one would let you close one that is not yours) |
| `force` | boolean |  | DISCARD uncommitted changes and close anyway (default false). That is the only thing it overrides and the only thing it costs: no branch holds those bytes, so nothing can bring them back. Unlanded commits never needed it — they are reported, not refused (#394) |

Removes the worktree. The branch survives — closing a workspace is not throwing work away.

```console
$ dot workspace_close as=bob
{"ok":true,"result":{"name":"bob","branch":"users/bob","path":"/path/to/state/dotworld/worktrees/handbook/bob","closed":true,"branchKept":true,"unlanded":0,"discarded":0,"detail":"The workspace directory is gone; users/bob is not. It had nothing main does not already have."}}
```

---

## `workspace_list`

List every working tree of this source, so you can find work that is not in front of you. The answer comes from `git worktree list`, so it is every tree git knows about and not only the ones dotworld created. Each carries its `branch` (git's answer, `null` on a detached HEAD), `path`, and what is in it: `dirty` (uncommitted files) and `unlanded` (commits its branch has that the canonical checkout does not — measured against whatever that checkout has checked out at the moment you ask, which is where `workspace_submit` merges). `kind` says what you may do with it. `managed` came from `workspace_open` and is the only kind `workspace_close` closes. `external` is a worktree made out of band — with plain `git worktree add`, very often by another agent's harness — which you can read and target with `workspace=` but not close here. `canonical` is the source's own checkout, listed because its uncommitted work is exactly what `workspace_submit` refuses on as `tracked-checkout-dirty` and it is very often your own. `locked` and `prunable` are git's own words when it has them: locked means a close will be declined, prunable means the registration points at a directory that is gone. Worktrees live outside the source root, so `ls` in the notes folder cannot see them and this is how you find them. Use it to see whether the work you are looking for is in another tree — including one opened from a different face, since under auth mode `none` the CLI is user `local` and the MCP server is user `mcp`, so they are on different branches by default. To then see that work, pass its path as `workspace` to `git` (`status`, `diff`, `log`); to build on it without waiting for them to land, its branch is an ordinary ref — `git subcommand=merge ref=<branch>` — because linked worktrees share one repository.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |

```console
$ dot workspace_list
{"ok":true,"result":[{"name":"source","branch":"main","path":"/path/to/handbook","kind":"canonical","dirty":0,"unlanded":0},{"name":"bob","branch":"users/bob","path":"/path/to/state/dotworld/worktrees/handbook/bob","kind":"managed","dirty":0,"unlanded":0}]}
```

---

## `workspace_open`

Create (or report) the calling user's isolated workspace on their own branch. ISOLATION IS A PROPERTY OF AUTHENTICATED DEPLOYMENTS: under auth mode `none` there is one identity, so every caller shares one workspace unless it names one with `as` — two agents on one no-auth server otherwise write to the same tree while each believes it is isolated. Note also that the identity differs by FACE under mode `none` (the CLI defaults to the identity of the workspace worktree its cwd is standing in, else `local`; the MCP server is `mcp`), so work opened on one face is on a branch the other does not look at; `branch` in the result is how you find out which. Whatever you get, thread the returned `workspace` path through every subsequent mutating call — nothing routes you there automatically without a declared auth mode.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `as` | string |  | name a workspace of your own (auth mode `none` ONLY; refused under a declared auth mode). It names a WORKSPACE, NOT AN IDENTITY: commits in it are still authored as the single local user, so `git log --author=<name>` will not find them. A deployment that wants isolation WITH attribution configures auth instead. |

An isolated worktree on `users/bob`, outside the source root. Bob works there; nobody edits the canonical checkout out from under him.

```console
$ dot workspace_open as=bob
{"ok":true,"result":{"path":"/path/to/state/dotworld/worktrees/handbook/bob","branch":"users/bob","created":true}}
```

---

## `workspace_submit`

Merge the calling user's branch into the canonical checkout, wherever it is standing. dotworld holds no landing target of its own: it resolves nothing, checks nothing out, and never moves your HEAD — it merges where you are and reports `target`, the branch it merged into (git's own `rev-parse --abbrev-ref HEAD`, so a detached checkout reads `HEAD`). To land somewhere else, `git checkout` there first. Divergence is reported the way git reports it and nothing more: status 'blocked' with blockedBy.reason 'needs-rebase' and the conflicted paths. There is no per-field conflict payload and nothing to submit back — resolve it in your own workspace, exactly as a shell user would: `git rebase`, read the marked file with file_read, send the reconciled text with file_stage, then `git rebase --continue` and workspace_submit again. Refused with blockedBy.reason 'dot-unparseable' when the branch carries a .dot.md dotworld cannot read (e.g. committed merge markers). Every refusal carries blockedBy.detail: one sentence naming the cause and the way out, and stating explicitly that nothing was landed and nothing was written — so re-running once you have acted on it is safe. Read it before touching the checkout: 'tracked-checkout-dirty' means the shared checkout holds uncommitted work, which is very often your own, and clearing it destroys the edits you were trying to submit. 'workspace-dirty' is the same hazard about your own workspace: dotworld never commits on your behalf, so uncommitted edits sitting there at the moment you submit are refused rather than silently left off the land — commit them (`git subcommand=commit`) and submit again. A resubmit of a branch the target already contains answers status 'up-to-date' with reason 'already-contained' (not a second 'merged' with a stale-looking commit hash) — 'contained', not 'landed', because what contains it is `target` and `target` moves with your checkout. An untracked `.dot.md` — blank or authored — blocks like any other uncommitted path: a read never mints one for you any more, so if one is sitting there it is real, and `git subcommand=rebase` never clears it out of the way either — commit it (`git subcommand=add` then `git subcommand=commit`) and submit again. A verdict carries `mediatedBy` only when a submit-wrapping plugin was attached, and it is provenance — the id of the plugin this call was dispatched to. It does not mean that plugin changed anything: the kernel performs the merge either way, and one that scanned, found nothing to do and did no work at all is stamped identically. Absent means no wrapping plugin was attached. Whatever else such a plugin did, it did in git — look for it with `git subcommand=branch`, not in this answer.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `message` | string |  | land message |

Both sides edited the same lines. The conflict is reported the way git reports one — status plus the conflicted **paths**. dotworld never hands back a per-field payload with both sides in it; you settle it in your own workspace.

```console
$ dot workspace_submit 'message=retention: shorten the log window'
{"ok":true,"result":{"status":"blocked","blockedBy":{"reason":"needs-rebase","paths":["notes/retention.md"],"generated":[],"detail":"Your branch has diverged from `main` on these paths. Nothing was landed and nothing was written. Resolve it where a shell user would — in your workspace: rebase onto it (`dot git subcommand=rebase ref=main`), read the marked file with `file_read`, send the reconciled text with `file_stage`, then continue the rebase and `workspace_submit` again. If git refuses the rebase citing an untracked `.dotworld/dots/*.dot.md` file, that dot is real uncommitted work: no read ever mints one any more, and nothing clears one out of the way for you (#383) — commit it (`git subcommand=add` then `git subcommand=commit`) or set it aside with `git subcommand=stash`, never delete it, then retry."}}}

ANSWERED FROM THE WORKSPACE /path/to/state/dotworld/worktrees/handbook/bob — resolved from your current directory, not from anything you passed. Pass `workspace=<path>` (see `workspace_list`) to choose a different tree explicitly.
```

With every conflicted path settled, the submit lands in the canonical checkout.

```console
$ dot workspace_submit 'message=retention: settle the log window at 90 days'
{"ok":true,"result":{"status":"merged","commit":"29f231432c6b94d369444301a34c5fa864626615","branch":"users/bob","target":"main"}}

ANSWERED FROM THE WORKSPACE /path/to/state/dotworld/worktrees/handbook/bob — resolved from your current directory, not from anything you passed. Pass `workspace=<path>` (see `workspace_list`) to choose a different tree explicitly.
```

---

## Summaries and obsolescence

## `obsolescence_review`

Find the notes a decision file has made out of date, and stage the corrections for review. Three steps: flag every note the kernel's impact graph says the decision touches (`graph_affected`, embedding plus graph), judge each candidate with this plugin's configured engine, and write confirmed corrections as unlanded commits on the `users/obsolescence` branch — the canonical checkout is never written to directly. The response says what happened: `candidates` lists everything flagged and why; `confirmed` lists the corrections actually staged, each with the judge's `reason`, `validUntil` and `score` (0–1 confidence, when the engine reported one); `branch` names the branch holding the staged commits, or `null` when nothing was confirmed. To accept the staged corrections, `git merge ref=users/obsolescence`; to discard them, `git branch -D` that same name.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL. Omitted, dotworld infers it from your working directory, exactly like every other command's sourceId. |
| `decisionPath` | string | yes | path to the decision file that may have aged out other notes — refused if it does not exist. |

React to a decision: which notes does this decision put in doubt?

```console
$ dot obsolescence_review decisionPath=decisions/2026-02-adopt-postgres.md
{"ok":true,"result":{"branch":null,"candidates":[{"path":"notes/retention.md","reason":"both"},{"path":"notes/incident-2026-03.md","reason":"both"},{"path":"notes/onboarding.md","reason":"both"},{"path":"notes/retention-pack.md","reason":"both"},{"path":"notes/deletion-runbook.md","reason":"both"}],"confirmed":[]}}
```

---

## `summary_refresh`

Rewrite the summaries that no longer describe their file, for the files you name. A summary is stale when the body changed after it was written (`summary_blob`, the same comparison `dot_get`'s `summaryStatus` reports) — the kernel detects that and has no opinion about what the summary should now say, because the kernel holds no model; this plugin asks its engine. Name what to refresh with `files` (paths, a directory like `docs/`, or `*` for the whole source), `index` (a document whose links are the list), or both — the index is appended after the files and deduped. A name that is both a file and a directory resolves to the file, because identity is the path. Naming neither refuses: there is no default, because the default would be rewriting every dot in the source. It refreshes, it does not author: a file with no summary yet (an `unread` placeholder) is left alone, and so is one whose summary is still current — naming a file asks for a refresh-if-stale, not an unconditional rewrite. Writes go straight to the dots in the working tree, uncommitted, and a rewritten summary is re-stamped against the body it now describes, so the file reads `current` and is not re-drafted next time. `refreshed` reports each one with the summary it replaced; review with `git diff` and commit them like any other edit. Nothing is branched, queued or committed on your behalf. A stale file whose redraft comes back identical to the summary already there is reported in `reverified`, not `refreshed`: the text is left alone and only the stamp is rewritten, recording that the engine read this body and confirmed it — so the file converges to `current` instead of being re-drafted, and paid for, on every later call. One file failing — a model that times out, a dot that will not parse, an engine that answers with nothing — does not abandon the rest: it lands in `failed` with its error, the run continues, that file stays stale so a later call retries it, and every path in `refreshed` was really written.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL. Omitted, dotworld infers it from your working directory (your own workspace counts), exactly like every other command's sourceId. |
| `files` | array of string |  | what to refresh — use instead of (or as well as) `index`. Four spellings, and they mix freely in one list: a file path (`"runbooks/deploy.md"`); a DIRECTORY, with or without a trailing slash (`"docs/"`), which selects the files under it; `"*"`, which selects the whole source; and a list of any of those. There is deliberately no separate `all` param — `"*"` is the spelling for the whole corpus. Over the CLI these are CWD-relative, exactly like `git add` (so `"*"` typed from a subdirectory means that subdirectory); MCP and HTTP always take them relative to the source root. A path that resolves to nothing is REPORTED in `missing`, never a refusal — the rest of the list is still refreshed. |
| `index` | string |  | a document whose LINKS are the list of files to refresh — use instead of (or as well as) `files`. Any document works: a generated index from `index_create`, or a hand-written note with links in prose. Read from the file every call, so pruning a line changes what gets refreshed, exactly like `file_read index=` / `context_load index=`. |
| `recursive` | boolean |  | does a DIRECTORY selection descend into subdirectories? Default true. Applies to `"*"` too, which is the source root as a directory: `files=["*"] recursive=false` refreshes only the files at the top level. No effect on a file named directly, or on `index`'s links. |
| `workspace` | string |  | The working tree to write to (default: the canonical checkout — see `property_set`'s `workspace` param, which this matches exactly). The dots are written here and left uncommitted: `git diff` is the review, and you commit them like any other edit. |

```console
$ dot summary_refresh 'files=["notes/incident-2026-03.md"]'
{"ok":true,"result":{"selected":1,"refreshed":[],"reverified":[],"skipped":{"stillCurrent":0,"unstamped":1},"failed":[],"missing":[]}}
```

---

## Version control

## `git`

Run git in your working tree. This is git, not a dotworld abstraction: the subcommands mean exactly what they mean in a shell, and the output is git's own. Use it to see a conflict (status, diff), resolve just the marked region and carry on (add, then rebase --continue / merge --continue), or inspect history (log, show). A non-zero exitCode is a normal answer, not a failure — a conflicted merge or rebase is left in place for you to edit, with the conflicted paths in `unmerged` and their diff in `diff`. Only an allowlisted set is available; anything else is refused with BAD_PARAMS. This is also how you see another agent at work: pass their workspace path (from `workspace_list`) as `workspace` and `status`/`diff`/`log` answer about their tree, including work they have not committed — the `tree` field says which tree answered. And because linked worktrees share one repository, their branch is an ordinary ref from anywhere: `ref=users/<name>` with `log`/`diff` to read it, with `merge`/`rebase` to build on it while they are still working. Nothing has to be landed first. An untracked `.dot.md` colliding with what `subcommand=rebase` would touch is never cleared out of the way — a read never mints one, so if one is there it is real, uncommitted work, and git refuses the whole rebase over it exactly as it would for any other untracked collision; commit it first.

| Param | Type | Required | Description |
| --- | --- | --- | --- |
| `sourceId` | string |  | source id — OPTIONAL for a caller with a working directory. The CLI infers it from your working directory, the way git infers the repository: a subdirectory works, and so does your own workspace (which lives outside the source root). A face with no working directory (this MCP server, or HTTP) can't infer one and refuses without it — pass it explicitly. Also pass it explicitly when you are outside every registered source, when two sources claim the same directory, or to act on a source you are not standing in. `source_list` names them. |
| `subcommand` | string | yes | git subcommand — one of: add, branch, commit, diff, log, merge, rebase, restore, show, status |
| `paths` | array of string |  | working-tree-relative paths (never escaping the tree). Over the CLI this is CWD-relative, exactly like plain `git` — MCP and HTTP always take it relative to the source root (#337). The bare source root (e.g. `.`) is a valid pathspec, same as `git add .`. |
| `ref` | string |  | branch, tag or revision (may not begin with "-") |
| `message` | string |  | commit / merge message |
| `flags` | array of string |  | per-subcommand allowlisted flags, e.g. --porcelain, --continue, --abort |
| `workspace` | string |  | working tree to act in (default: yours — see workspace_open). Under a declared auth mode this is still honoured verbatim: naming another identity's workspace path routes the write into THAT identity's branch, not yours, regardless of who is authenticated — an explicit workspace outranks identity, by design (docs/multi-user.md, R21). Only pass a path you opened yourself or were handed. |

```console
$ dot git subcommand=status
{"ok":true,"result":{"subcommand":"status","args":["--no-pager","status"],"tree":"/path/to/handbook","exitCode":0,"stdout":"On branch main\nUntracked files:\n  (use \"git add <file>...\" to include in what will be committed)\n\t.dotworld/connectors/\n\t.dotworld/dots/\n\t.dotworld/properties.json\n\tnotes/incident-2026-03.md\n\tnotes/retention-pack.md\n\nnothing added to commit but untracked files present (use \"git add\" to track)\n","stderr":""}}
```

dotworld never commits on your behalf. Stage the dots alongside the files they describe — `.dotworld/dots/` is durable and belongs in the same commit as the source change.

```console
$ dot git subcommand=add 'paths=["."]'
{"ok":true,"result":{"subcommand":"add","args":["--no-pager","add","--","."],"tree":"/path/to/handbook","exitCode":0,"stdout":"","stderr":""}}
```

This is git, not an abstraction over it. `exitCode` is git's own — a commit that stages nothing exits 1, and the envelope still says `ok: true` because git ran fine.

```console
$ dot git subcommand=commit 'message=handbook: retention metadata and the March incident'
{"ok":true,"result":{"subcommand":"commit","args":["--no-pager","commit","-m","handbook: retention metadata and the March incident"],"tree":"/path/to/handbook","exitCode":0,"stdout":"[main 8dca7e2] handbook: retention metadata and the March incident\n 8 files changed, 71 insertions(+)\n create mode 100644 .dotworld/connectors/supersedes%3Anotes%2Fretention.md-%3Edecisions%2F2026-02-adopt-postgres.md.json\n create mode 100644 .dotworld/dots/notes/deletion-runbook.md.dot.md\n create mode 100644 .dotworld/dots/notes/incident-2026-03.md.dot.md\n create mode 100644 .dotworld/dots/notes/retention-pack.md.dot.md\n create mode 100644 .dotworld/dots/notes/retention.md.dot.md\n create mode 100644 .dotworld/properties.json\n create mode 100644 notes/incident-2026-03.md\n create mode 100644 notes/retention-pack.md\n","stderr":""}}
```

```console
$ dot git subcommand=log 'flags=["--oneline"]'
{"ok":true,"result":{"subcommand":"log","args":["--no-pager","log","--oneline"],"tree":"/path/to/handbook","exitCode":0,"stdout":"8dca7e2 handbook: retention metadata and the March incident\n7e27caf handbook: initial notes\n","stderr":""}}
```

Resolve it where a shell user would. The rebase stops on the conflict and exits non-zero — that is git's own exit code, surfaced rather than swallowed.

```console
$ dot git subcommand=rebase ref=main
{"ok":true,"result":{"subcommand":"rebase","args":["--no-pager","rebase","main"],"tree":"/path/to/state/dotworld/worktrees/handbook/bob","exitCode":1,"stdout":"Auto-merging notes/retention.md\nCONFLICT (content): Merge conflict in notes/retention.md\n","stderr":"Rebasing (1/1)\rerror: could not apply b1e047b... retention: shorten the log window to 30 days\nhint: Resolve all conflicts manually, mark them as resolved with\nhint: \"git add/rm <conflicted_files>\", then run \"git rebase --continue\".\nhint: You can instead skip this commit: run \"git rebase --skip\".\nhint: To abort and get back to the state before \"git rebase\", run \"git rebase --abort\".\nhint: Disable this message with \"git config set advice.mergeConflict false\"\nCould not apply b1e047b... # retention: shorten the log window to 30 days\n","unmerged":["notes/retention.md"],"diff":"diff --cc notes/retention.md\nindex 482e1c7,685586f..0000000\n--- a/notes/retention.md\n+++ b/notes/retention.md\n@@@ -1,6 -1,6 +1,10 @@@\n  # Data retention policy\n  \n++<<<<<<< HEAD\n +We keep customer event logs for 180 days, then delete them. Billing records are\n++=======\n+ We keep customer event logs for 30 days, then delete them. Billing records are\n++>>>>>>> b1e047b (retention: shorten the log window to 30 days)\n  kept for seven years, because the tax authority can ask for them that far back.\n  \n  Deletion is a hard delete, not a flag: see [[notes/deletion-runbook.md]].\n"}}
```

```console
$ dot git subcommand=rebase 'flags=["--continue"]'
{"ok":true,"result":{"subcommand":"rebase","args":["--no-pager","rebase","--continue"],"tree":"/path/to/state/dotworld/worktrees/handbook/bob","exitCode":0,"stdout":"[detached HEAD fb377ec] retention: shorten the log window to 30 days\n 1 file changed, 1 insertion(+), 1 deletion(-)\n","stderr":"","unmerged":[]}}
```

---

## This install

## `embedding_info`

The active semantic-search model, and whether it is the one that was configured. Returns { engine, dim, quality, available, requested, honored, reason, note, configFile, build, maxTokens }. `quality` is the one field that answers "is search healthy right now" — 'ok' or a 'degraded-…' value — and it is keyed on the live engine, not on `reason` alone: `reason` says why a request was disobeyed ('unknown-engine' — bad name or a stale build; 'construct-failed' — package or weights missing, which is also what `auto` reports when it finds no engine it can build). Read `quality` first: it is the one field that answers "can this install search", and it keys on whether a model is actually loaded rather than on whether the config was obeyed. Those are different questions — `auto` with no pulled weights is `honored: true` and cannot search — and `requested: null, honored: true, reason: null` is byte-identical whether no engine was ever configured or a real dotworld.config.json simply was not found from this cwd (config discovery is cwd-sensitive, docs/plugins.md), which is why the reason fields alone cannot be trusted as a health signal. `configFile` names which file (if any) produced this answer, and `note` says so in prose. `maxTokens` (null when the engine has no such ceiling) is how many tokens the model sees in one pass, but a longer file is split into overlapping chunks and every part of it is embedded, so this is a chunk size, not a horizon past which the file is unseen. There is no fallback ranking: when no model is loaded `search_semantic` refuses, so use search_grep for exact identifiers and search_properties for metadata. This is about embeddings only: for the build stamp, the node version and the command set ask `version`, and for whether the install is healthy at all ask `sync_doctor`.

_No parameters._

```console
$ dot embedding_info
{"ok":true,"result":{"engine":"fastembed","dim":384,"quality":"ok","available":["fastembed"],"requested":"fastembed","honored":true,"reason":null,"configFile":"/path/to/handbook/.dotworld/dotworld.config.json","note":"Search is running on 'fastembed', exactly as configured ('fastembed'). Config file in effect for this process: /path/to/handbook/.dotworld/dotworld.config.json.","maxTokens":512}}
```

---

## `plugin_list`

Report which plugins this install has attached, and which were skipped at boot and why. `commands` lists the commands those plugins registered through the `contributesCommands` seam, along with any skipped because the name was already taken. A command a plugin contributes doesn't exist at all when that plugin is not attached — it is not merely inert. `loaded: []` alone can't distinguish a repo that configured no plugins from one whose dotworld.config.json simply was not found from this cwd (config discovery is cwd-sensitive, docs/plugins.md) — `configFile`/`note` name which file (if any) produced this answer.

_No parameters._

```console
$ dot plugin_list
{"ok":true,"result":{"loaded":[{"id":"obsolescence","version":"1.0.0","engine":"rule-based"},{"id":"summarizer","version":"1.0.0","engine":"rule-based"},{"id":"comms","version":"1.0.0","engine":"fs-log"},{"id":"context-loader","version":"1.0.0","engine":"compose"}],"skipped":[],"commands":{"registered":[{"name":"obsolescence_review","pluginId":"obsolescence"},{"name":"summary_refresh","pluginId":"summarizer"},{"name":"comms_send","pluginId":"comms"},{"name":"comms_read","pluginId":"comms"},{"name":"context_load","pluginId":"context-loader"}],"skipped":[]},"configFile":"/path/to/handbook/.dotworld/dotworld.config.json","note":"Config file in effect for this process: /path/to/handbook/.dotworld/dotworld.config.json."}}
```

---

## `version`

Which build is answering you, which search engine it is running, and which commands it serves. Returns { build, node, engine, commands, note }. `build.stale: true` means the running build is behind its source and every face is serving old code — rebuild (npm run build); `build.stale: null` means freshness is NOT KNOWABLE here (running from source, or an installed package with no src/ to compare against) and must not be read as fresh. `commands` is the command set itself, so a client written against an older set can ask directly whether what it needs is still served rather than discovering it one UNKNOWN_COMMAND at a time. For whether the install is HEALTHY — merge driver, stranded dots, dead worktree registrations — ask sync_doctor.

_No parameters._

```console
$ dot version
{"ok":true,"result":{"build":{"builtAt":"<varies>","stale":false,"rev":"<varies>"},"node":"<varies>","engine":{"name":"fastembed","dim":384,"quality":"ok","requested":"fastembed","honored":true},"commands":["comms_read","comms_send","context_load","dot_attach","dot_get","dot_list","dot_set","embedding_info","file_create","file_delete","file_read","file_stage","file_tree","git","graph_affected","graph_connect","graph_disconnect","graph_get","graph_provenance","index_create","obsolescence_review","plugin_list","property_get","property_list","property_register","property_set","property_unregister","property_unset","search_grep","search_properties","search_semantic","search_triage","source_list","source_register","source_unregister","source_update","summary_refresh","sync_doctor","sync_merge","sync_source","version","workspace_close","workspace_list","workspace_open","workspace_submit"],"note":"dotworld build <varies>, node <varies>, search on 'fastembed', 45 commands."}}
```

---

## The CLI's own subcommands

These four are the CLI itself, not dispatched commands: they never reach a handler, they
take no `sourceId`, and they are absent from `dot catalog` and from every MCP client —
which is why they have no parameter table here. They are still what `dot --help` lists.

### `commands`

List every dispatchable command with its one-line description — the set this page documents, from the terminal.

```console
$ dot commands
comms_read           Read comms addressed to `as` (`to === as` or `to === 'all'`), oldest first. With `since` omitted (the common case) this resumes from a persisted per-reader cursor and ADVANCES it to the last message returned before returning — each message is delivered to a given `as` exactly once across calls, not re-scanned on every poll. Pass `since` explicitly to replay from an earlier point instead; an explicit `since` never touches the stored cursor. The response carries BOTH: `since` is what this call filtered from (your input echoed back on an explicit-`since` call, the advanced cursor on a default one), and `cursor` is the persisted pointer — where this `as` actually stands, and the point the next default call resumes from. Read `cursor`, not `since`, to tell a caught-up mailbox from a `since` that skipped a backlog nothing has consumed: both answer `messages: []`, and only `cursor` distinguishes them. To ABANDON a backlog without receiving it, pass `advance=true` (or `seek=<n>`): the cursor moves and NO messages are returned — the one deliberate hole in "a read is a consume". `since`, `seek` and `advance` are mutually exclusive; passing more than one is refused rather than ranked.
                     sourceId:string  as*:string  since:number  seek:number  advance:boolean
comms_send           Append one message to the source-wide comms log — cross-workspace messaging between sibling `workspace_open` agents on the same source. Not durable knowledge: this is scratch coordination, disposable like the rest of `.dotworld/derived/`, never a substitute for landing real work through `workspace_submit`. Every worktree of the source sees every message — storage is rooted at the source, not your own workspace.
                     sourceId:string  from*:string  to*:string  topic*:string  body*:string
context_load         Join a list of files into one prompt-ready blob of context. Because the list can be an index document, this works as a dynamic alternative to a static CLAUDE.md or agent-definition file: point `index` at a document and edit that document to change what loads next time, with nothing to redeploy. `mode=file` joins full file bodies (`file_read`); `mode=dot` joins each dot (`dot_get`) — title, tags, summary, authored body. Same resolution as those two commands: `files`, `index`, or both (index appended after files, deduped) — naming neither refuses. `entries` carries the raw per-file rows (exactly what `file_read`/`dot_get` would answer) for a caller that wants to filter or reorder; `context` is the same information already joined into one string, in resolution order, ready to drop into a prompt. `missing` names anything that did not resolve (not found, or a dot that could not be parsed) — never a refusal. `mode=file` also reports `binary`: file paths whose bytes are not text, skipped from the blob rather than lossily decoded, exactly like `file_read`. Adds nothing `file_read`/`dot_get` do not already compute — a formatting convenience over their output, never a third source of truth.
                     sourceId:string  files:array  index:string  mode*:string  workspace:string
dot_attach           Attach a stranded dot to a file — the manual half of rename reconciliation, for the move it could not pair. It relocates one dot; it does not relate two things, and it authors no edge — `graph_connect` is that command. Both params name files, never `.dot.md` paths: `dot` is the path the stranded dot still describes (as `dot_list orphaned=true` reports it), `file` is the file to attach it to. This does everything a detected rename does — rewrites the dot's `id` and `source_path`, moves the dot, and rewrites authored connectors naming the old path — which is why hand-moving the `.dot.md` is not equivalent: that silently severs every curated edge. Nothing is merged: the dot you name wins outright, because the reason you are here is that automatic pairing already guessed wrong once. Four refusals. (1) The named dot's own file still exists — nothing is stranded; move the file instead and the next read carries its dot. (2) `file` is not a file in this working tree. (3) The destination already has an authored dot — pass overwrite=true to replace it; a blank one, such as the one an interim read minted at the new path, is replaced without asking. A title counts as authored when dotworld did not mint it (a minted one is the filename stem or the file's own first H1), so a destination carrying nothing but a title somebody wrote is refused like any other; the refusal names it, and overwrite is all-or-nothing, so re-author it with `dot_set` afterwards if you wanted it kept. (4) Either dot is unparseable. The two halves of (4) differ, deliberately: a defective dot at the destination is replaceable with overwrite=true, because it is being discarded wholesale, but a defective dot at `dot` is refused outright and overwrite=true will not clear it — its `id` and `source_path` cannot be rewritten without reading it, so moving it would leave a broken dot describing a different file. Repair that one by hand first; the refusal names the repair.
                     sourceId:string  dot*:string  file*:string  overwrite:boolean  workspace:string
dot_get              Read what dotworld knows about a file: the whole dot by default, or named components via `fields`. A dot carries title, tags, properties and an authorable body; the answer adds derived signals (centrality, summaryStatus). `centrality` is PageRank over the connector graph with `supersedes` edges left out — an edge recording that a document is dead is not evidence it matters — so a superseded file ranks on its other links alone. This is the triage step: it tells you whether a file is worth opening without paying for the file's body (that is `file_read`). The dot body is included by default — it is authored scratch notes about the file and is usually empty, so it costs nothing like `file_read`; `fields=["body"]` reads it alone. Name the files with `files`, or point `index` at a document and every file it links to is the list. Addressed by the file's path: identity is the path, and a dot is never addressed by its own. `dot_set` is the matching writer. For properties, prefer `property_get`: this command's projection omits a field the dot does not carry, so an absent property is indistinguishable from one you did not ask for — `property_get` names it in `missing` instead. `properties` is the trap, because it is not a field at all: property keys are spread at the top level of the answer, so `fields=["properties"]` answers `{"path":"…"}` for every file, always — name the property keys you want.
                     sourceId:string  files:array  index:string  fields:array  workspace:string
dot_list             Every dot in this source as an object: the file it describes, where the `.dot.md` itself lives, and whether that file is still here. `orphaned=true` is the one that matters — a dot whose file is absent from the working tree. An orphan is durable, in git, and holds real authored metadata, and nothing else in the command set can see it: `file_tree` lists files, and every aggregate over dots (`search_properties`, `property_list` counts) is computed over live files only, so a property that exists solely on an orphan reads as used by nobody. A dot is orphaned in two ways, and the second is the common one: a file deleted on purpose leaves its dot behind (retire it with `git rm` of the `dotPath` here), and a file moved out of band leaves it behind when reconciliation could not pair the two halves (reattach it with `dot_attach`) — see `sync_source`. An orphan row carries the metadata stranded on it, so you can judge whether it is worth recovering without opening anything, plus `hint`/`hintReason`: where the file probably went, advisory and never acted on. Weigh the three reasons differently — `git-rename-in-the-deleting-commit` is git's own pairing of the two paths; `closest-embedding` is the nearest of that commit's arrivals to the orphan's last committed content in vector space, carrying `hintScore` (cosine, 0-1), which is how you tell a continuation from a coincidence; `the-only-file-added-in-that-commit` is mere co-occurrence: `git rm obsolete.md`, add `changelog.md`, commit, and the changelog is hinted, with nothing whatever to do with the file that left — and its `hintScore`, when present, is what says so. A near-tie gives no `hint` and a `hintTie` listing the candidates with their scores instead, because naming one would be a coin toss dressed as evidence. `semanticHints` says whether the embedding hint ran at all: with no embedding model loaded it declines rather than guessing — ask `embedding_info` why the model is not running. `orphaned` counts the stranded dots whether or not you filtered for them, so a plain call answers "is anything stranded?" in one go. The second state is `indexed`, and it is a different condition with a different repair: a file that is here (`exists: true`) yet is outside what this source tracks — which is what a file that acquired a dot and was then gitignored looks like. Its metadata is invisible to every query for the same reason an orphan's is, so `notIndexedReason` says why and `ignoredBy` names the rule as git names it: the ignore file, the line and the pattern. The repair is to change that rule or move the file, and it is neither orphan repair — nothing is stranded, `dot_attach` has nothing to reattach, and `git rm` would throw the metadata away. `notIndexed` is counted separately from `orphaned` for exactly that reason, and is likewise reported whether or not you filtered.
                     sourceId:string  orphaned:boolean  notIndexed:boolean  match:string  limit:number  workspace:string
… 74 more lines of output
```

### `catalog`

Print the MCP tool catalog as JSON: every command, its description and its full parameter schema. This is what an MCP client reads, and what generates this page.

```console
$ dot catalog
[
  {
    "name": "comms_read",
    "description": "Read comms addressed to `as` (`to === as` or `to === 'all'`), oldest first. With `since` omitted (the common case) this resumes from a persisted per-reader cursor and ADVANCES it to the last message returned before returning — each message is delivered to a given `as` exactly once across calls, not re-scanned on every poll. Pass `since` explicitly to replay from an earlier point instead; an explicit `since` never touches the stored cursor. The response carries BOTH: `since` is what this call filtered from (your input echoed back on an explicit-`since` call, the advanced cursor on a default one), and `cursor` is the persisted pointer — where this `as` actually stands, and the point the next default call resumes from. Read `cursor`, not `since`, to tell a caught-up mailbox from a `since` that skipped a backlog nothing has consumed: both answer `messages: []`, and only `cursor` distinguishes them. To ABANDON a backlog without receiving it, pass `advance=true` (or `seek=<n>`): the cursor moves and NO messages are returned — the one deliberate hole in \"a read is a consume\". `since`, `seek` and `advance` are mutually exclusive; passing more than one is refused rather than ranked.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "sourceId": {
          "type": "string",
          "description": "source id — OPTIONAL. Omitted, dotworld infers it from your working directory (your own workspace counts), exactly like every other command's sourceId."
        },
        "as": {
          "type": "string",
          "description": "the mailbox to read as — matches messages sent `to` this name or to 'all', and names the persisted cursor."
… 1300 more lines of output
```

### `completion`

Emit a tab-completion script for your shell (`bash`, `zsh` or `fish`). Source it, or write it into your shell profile.

```console
$ dot completion bash
# dotworld completion for bash — generated by `dot completion bash`.
#
# The command set is baked in: regenerate after upgrading dotworld.
#   dot completion bash > ~/.local/share/bash-completion/completions/dot
# or, to load it from your ~/.bashrc:
#   source <(dot completion bash)

__dot_commands='catalog commands comms_read comms_send completion context_load dot_attach dot_get dot_list dot_set embedding_info file_create file_delete file_read file_stage file_tree git graph_affected graph_connect graph_disconnect graph_get graph_provenance index_create obsolescence_review plugin_list property_get property_list property_register property_set property_unregister property_unset search_grep search_properties search_semantic search_triage source_list source_register source_unregister source_update summary_refresh sync_doctor sync_merge sync_source version workspace_close workspace_list workspace_open workspace_submit'

# Complete a filesystem path for a param that names one.
… 128 more lines of output
```

### `help`

Usage for one command — its description and every flag it takes. `dot --help` on its own lists them all.

```console
$ dot help search_semantic
Usage: dot search_semantic [options] [pairs...]

Search the source by meaning: describe the task or question and get back the
files most likely to answer it, ranked. A hit carrying dotUnparseable: true has
unreadable metadata — its summary/summaryStatus are placeholders, not facts; do
not read "unread" as "nobody summarised it". `obsolete`/`superseded` are stated
on every hit — check them before reading: a superseded file can still rank
above its replacement, and this is the only reliable way to tell. The top of
the ranking is reranked with lexical query/document overlap before `limit` is
applied, which corrects a known failure mode where whole-document embedding
leaves several candidates nearly tied — this narrows near-ties, it does not
override a decisive semantic match. Refuses with BAD_PARAMS, rather than
silently ranking on a weaker fallback, when dotworld.config.json names a
specific embedding engine that could not be built — engine: "auto" degrades
… 16 more lines of output
```

---
